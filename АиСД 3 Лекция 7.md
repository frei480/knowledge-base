# Лекция 7: Быстрое Преобразование Фурье (FFT)

## 1. Постановка Задачи
Изначальная мотивация лекции — решение задачи **нечеткого поиска подстроки**: найти все вхождения строки $S$ в текст $T$, где количество несовпадений не превышает $k$.
В «лоб» это решается долго. Чтобы решить это эффективно (за время, близкое к линейному или $O(N \log N)$), нам понадобится инструмент для быстрого перемножения многочленов.

### Задача Умножения Многочленов
Даны два многочлена $P(x)$ и $Q(x)$ степени $N$. Нужно найти их произведение $R(x) = P(x) \cdot Q(x)$.
*   **Наивный подход:** перемножение «каждый с каждым» — $O(N^2)$.
*   **Цель:** научиться делать это за **$O(N \log N)$**.

---

## 2. Теоретическая База FFT

### Представления Многочлена
1.  **Коэффициентное:** список коэффициентов $[a_0, a_1, \dots, a_n]$. Умножение сложное.
2.  **По значениям в точках:** набор пар $\{(x_0, y_0), \dots, (x_k, y_k)\}$.
    *   Умножение в этом виде тривиально: за $O(N)$ просто перемножаем значения в соответствующих точках ($y_i^R = y_i^P \cdot y_i^Q$).
    *   **Утверждение (Интерполяция):** Многочлен степени $N$ однозначно восстанавливается по своим значениям в $N+1$ точке.

### Общая Схема Алгоритма
1.  **Evaluation (Вычисление):** Выбрать $K$ точек (где $K \ge \deg(P) + \deg(Q)$) и вычислить значения многочленов в них.
2.  **Point-wise Multiplication:** Перемножить значения за $O(K)$.
3.  **Interpolation (Интерполяция):** Восстановить коэффициенты многочлена-результата по значениям.

Сложность кроется в шагах 1 и 3. FFT позволяет делать их быстро.

### Выбор Точек: Комплексные Корни Из Единицы
Чтобы вычисления были быстрыми, выбирают не случайные точки, а **комплексные корни $n$-й степени из единицы**.
*   Корень $n$-й степени — это число $\omega$, такое что $\omega^n = 1$.
*   Главный корень: $\omega_n = e^{i \frac{2\pi}{n}} = \cos(\frac{2\pi}{n}) + i\sin(\frac{2\pi}{n})$.
*   Точки на комплексной плоскости: $\omega_n^0, \omega_n^1, \dots, \omega_n^{n-1}$.

---

## 3. Алгоритм FFT (Схема «Разделяй И властвуй»)

Чтобы посчитать значение многочлена $P(x)$ во всех корнях $\omega_n^k$, применяется рекурсивный подход.

### Идея
Разделим коэффициенты $P(x)$ на четные и нечетные:
$$P(x) = (a_0 + a_2x^2 + \dots) + x(a_1 + a_3x^2 + \dots)$$
$$P(x) = P_{even}(x^2) + x \cdot P_{odd}(x^2)$$

Если мы подставляем в $P(x)$ корни $n$-й степени ($\omega_n$), то в $P_{even}$ и $P_{odd}$ аргументом идет $x^2$.
**Ключевое свойство:** Квадраты корней $n$-й степени являются корнями $(n/2)$-й степени. Множество точек сокращается вдвое.

### Рекуррентная Формула
Для вычисления FFT массива размера $n$:
1.  Рекурсивно вычисляем FFT для четной половины коэффициентов ($P_{even}$) и нечетной ($P_{odd}$) (размер задач $n/2$).
2.  Объединяем результаты:
    $$P(\omega_n^k) = P_{even}(\omega_{n/2}^k) + \omega_n^k \cdot P_{odd}(\omega_{n/2}^k)$$
    $$P(\omega_n^{k+n/2}) = P_{even}(\omega_{n/2}^k) - \omega_n^k \cdot P_{odd}(\omega_{n/2}^k)$$
    *(Второе уравнение использует свойство $\omega_n^{k+n/2} = -\omega_n^k$)*.

Сложность: $T(N) = 2T(N/2) + O(N) \Rightarrow O(N \log N)$.

---

## 4. Обратное Преобразование (Inverse FFT)

Как по значениям восстановить коэффициенты?
Матричное уравнение для прямого FFT — это умножение вектора коэффициентов на матрицу Вандермонда $W$ (где элементы — степени $\omega$).
Обратное преобразование выполняется умножением на $W^{-1}$.

**Свойство:** Обратная матрица $W^{-1}$ выглядит почти так же, как прямая, но:
1.  Вместо $\omega$ берется $\omega^{-1}$ (сопряженное число, или $\omega$ в степени с минусом).
2.  Результат нужно поделить на $N$.

**Алгоритм:** Используем ту же функцию FFT, передав ей $\omega^{-1}$, а в конце делим все элементы массива на $N$.

---

## 5. Реализация И Оптимизации

### Итеративная Схема (без рекурсии)
Рекурсия дает накладные расходы по памяти и времени. Можно развернуть алгоритм в цикл.
1.  **Битовый реверс (Bit-reversal permutation):**
    На нижнем уровне рекурсии (листьях) элементы массива переставляются определенным образом. Индекс $i$ переходит в позицию $rev(i)$, где $rev(i)$ — число, полученное записью битов $i$ в обратном порядке.
    *Пример:* $6 (110_2) \to 3 (011_2)$.
    Нужно сделать `swap(a[i], a[rev[i]])`.
2.  **Бабочка (Butterfly):**
    Поднимаемся снизу вверх (объединяем блоки размером 1, 2, 4, 8…).
    Вычисления происходят *in-place* (на том же массиве), используя формулу объединения:
    ```
    x = a[i]
    y = a[i+len/2] * w
    a[i] = x + y
    a[i+len/2] = x - y
    ```

### Точность И Modular Arithmetic (NTT)
*   **Проблема:** При использовании `complex<double>` накапливается ошибка округления. Обычно приемлемо до коэффициентов порядка $10^{12}-10^{14}$.
*   **Решение (NTT):** Работа в поле вычетов по модулю простого числа $p$.
    *   Нужен модуль вида $p = c \cdot 2^k + 1$ (чтобы существовали корни степени $2^k$).
    *   Популярный модуль: $998244353$.
    *   Вместо $e^{i\dots}$ используется примитивный корень $g$ такого порядка, что $g^n \equiv 1 \pmod p$.
    *   Все операции (сложение, умножение) выполняются по модулю. Ошибок округления нет.

---

## 6. Применение: Поиск Подстроки С Ошибками

Возвращаемся к задаче: строка $S$ (длина $M$), текст $T$ (длина $N$). Найти вхождения с $\le k$ ошибками.

### Метод Через Свертку
Мы хотим посчитать количество совпадений символов для каждой позиции прикладывания $S$ к $T$.

1.  **Кодирование:**
    Перебираем каждый символ алфавита $\alpha \in \Sigma$.
    Создаем бинарные векторы для текста и шаблона:
    *   $T_{\alpha}[i] = 1$, если $T[i] == \alpha$, иначе 0.
    *   $S_{\alpha}[i] = 1$, если $S[i] == \alpha$, иначе 0.

2.  **Разворот шаблона:**
    Скалярное произведение для всех сдвигов — это **корреляция**. FFT считает **свертку** ($c_k = \sum a_i b_{k-i}$).
    Чтобы свести задачу к свертке, **развернем строку $S$**.
    Теперь при перемножении многочленов (построенных на $T$ и $S_{rev}$) коэффициент при степени $x^k$ будет содержать сумму произведений, соответствующих наложению шаблона на текст со сдвигом.

3.  **Алгоритм:**
    *   Для каждого символа $\alpha$ из алфавита:
        *   Формируем полиномы для $T$ и $S^{rev}$ (где 1 — совпадение символа).
        *   Перемножаем их с помощью FFT.
        *   Результат добавляем в массив счетчиков совпадений.
    *   После прохода по всем символам: если в позиции $j$ сумма совпадений $\ge |S| - k$, значит, вхождение найдено.

### Оптимизация (уменьшение Вызовов FFT)
Сейчас нужно $3 \cdot |\Sigma|$ вызовов FFT. Это долго, если алфавит большой.
Можно объединять символы парами.
Пусть считаем для символов 'a' и 'b'.
*   Полином $A$ для текста: ставим $1$ где 'a', и $M$ (большое число) где 'b'? Нет, лучше так:
*   Используем свойства комплексных чисел или линейность.
*   **Трюк:** Закодируем 'a' как $1$, а 'b' как $i$ (мнимая единица) или другой коэффициент. Можно за один проход FFT посчитать совпадения сразу для нескольких символов, если грамотно разнести их по вещественной и мнимой частям или использовать разные базисы.
    *   *Вариант из лекции:* Можно закодировать один символ как $1$, другой как $-1$. Тогда произведение $(1)(-1)$ даст вклад в минус, $(1)(1)$ в плюс. Можно восстановить количества через систему уравнений. Асимптотика улучшается до $O(\frac{|\Sigma|}{2} N \log N)$.

---
**Итог:** Мы научились перемножать многочлены за $O(N \log N)$ с помощью FFT и применили это для быстрого поиска подстрок с учетом несовпадений, сведя задачу к вычислению свертки бинарных векторов.