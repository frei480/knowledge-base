# Лекция 13: Глобальный Минимальный Разрез
## 1. Глобальный Минимальный Разрез

### Постановка Задачи

* Дан **неориентированный граф** с неотрицательными пропускными способностями рёбер.
* **Разрез** — разбиение множества вершин (V = S \sqcup T), где:

  * (S \neq \varnothing), (T \neq \varnothing);
  * стоимость разреза — сумма capacities рёбер между (S) и (T).
* **Глобальный минимальный разрез** — разрез минимальной стоимости **без фиксированных вершин (s,t)**.

Отличие от min-cut в потоках:

* В потоках фиксируются (s) и (t).
* Здесь перебираются **все возможные разбиения вершин**.

---

## 2. Идея Алгоритма Штор–Вагнера

### Наивное Решение

* Перебрать все пары вершин (s,t).
* Для каждой пары найти минимальный (s)-(t) разрез через max-flow.
* Сложность слишком большая: (O(n^2 \cdot \text{max-flow})).

### Ключевая Идея: **склеивание вершин**

Пусть найден минимальный разрез между вершинами (s) и (t).

Возможны два случая:

1. Этот разрез — **глобальный минимальный**.
2. В глобальном минимальном разрезе (s) и (t) лежат **в одной доле**.

Во втором случае:

* Вершины (s) и (t) можно **склеить** в одну.
* Все рёбра объединяются, capacities суммируются.
* Величина глобального минимального разреза **не меняется**.

➡️ Значит, можно уменьшать граф, не теряя ответа.

---

## 3. Общая Схема Алгоритма

Повторять, пока в графе больше одной вершины:

1. Найти пару вершин (s,t), для которых легко найти min-cut.
2. Обновить ответ как минимум из всех найденных разрезов.
3. Склеить (s) и (t).
4. Запустить алгоритм заново на уменьшенном графе.

---

## 4. Как Выбирать Вершины (s) И (t)

### Построение Специальной Перестановки Вершин

Строится порядок (a_1, a_2, \dots, a_n):

1. (a_1) — произвольная вершина.
2. На шаге (i):

   * выбирается вершина (v \notin {a_1,\dots,a_{i-1}}),
   * максимизирующая
     [
     C(v, {a_1,\dots,a_{i-1}})
     ]
     — суммарную capacity рёбер к уже выбранным.

Это **жадный алгоритм**, похожий на Прима / Дейкстру, но с максимумом.

---

## 5. Главное Утверждение

Пусть построена такая перестановка.

**Тогда:**

* Минимальный разрез между вершинами
  [
  a_{n-1} \quad \text{и} \quad a_n
  ]
  равен
  [
  C(a_n, {a_1,\dots,a_{n-1}})
  ]
* Этот разрез имеет вид:

  * (T = {a_n}),
  * (S = V \setminus {a_n}).

➡️ Мы знаем min-cut **без запуска max-flow**.

---

## 6. Корректность (идея доказательства)

### Активные Вершины

Вершина (a_i) называется **активной**, если:

* (a_i) и (a_{i-1}) лежат в разных долях фиксированного разреза (S|T).

### Лемма

Для любой активной вершины:
[
C(a_i, {a_1,\dots,a_{i-1}})
\le
C(S \cap A_i,; T \cap A_i)
]

где (A_i = {a_1,\dots,a_i}).

### Доказательство

* Индукция по активным вершинам.
* Используются:

  * жадный выбор вершины,
  * неотрицательность capacities,
  * факт, что учитывается лишь подмножество рёбер разреза.

### Следствие

Последняя вершина (a_n) **всегда активна**, значит:
[
\text{любой разрез} \ge C(a_n, {a_1,\dots,a_{n-1}})
]

А такой разрез существует → он минимальный.

---

## 7. Сложность

* Построение одной перестановки: (O(n^2)).
* Запусков алгоритма: (n-1) (каждый раз склеивание).
* **Итого:**
  [
  O(n^3)
  ]

Возможны оптимизации:

* Использование кучи → (O(nm \log n)) для разреженных графов.

---

## 8. Итог По Алгоритму Штор–Вагнера

* Находит **глобальный минимальный разрез**.
* Работает для **неориентированных графов**.
* Не использует max-flow.
* Основан на:

  * жадной перестановке вершин,
  * склеивании,
  * структурном свойстве min-cut.

---

## 9. Начало Следующей Темы: Потоки Минимальной Стоимости (кратко)

* На рёбрах есть:

  * capacity,
  * cost — цена за единицу потока.
* Задача: найти поток величины (K) минимальной стоимости.
* Используются обратные рёбра с отрицательной стоимостью.
* Базовый алгоритм:

  * (K) раз находить кратчайший путь по стоимости в остаточной сети.
* Корректность при отсутствии отрицательных циклов.


