# Лекция 7: Кратчайшие Пути В Графах (продолжение)

В лекции рассматриваются модификации алгоритма Дейкстры (двусторонний, A*) и алгоритм Форда-Беллмана для работы с отрицательными весами.

---

## 1. Двусторонний Алгоритм Дейкстры

**Задача:** Найти расстояние между конкретными вершинами $S$ и $T$ в графе с неотрицательными весами.

### Идея
Запустить поиск параллельно (или попеременно) с двух сторон:
1.  **Прямой поиск:** от $S$ по исходящим рёбрам.
2.  **Обратный поиск:** от $T$ по входящим (обратным) рёбрам.

### Алгоритм
*   Поддерживаем два массива расстояний ($D_S$ и $D_T$) и две кучи (приоритетные очереди).
*   На каждом шаге выбираем вершину с минимальным текущим расстоянием из двух куч и раскрываем её.
*   **Условие остановки:** Алгоритм завершается, когда некоторая вершина $V$ оказывается **раскрыта** (обработана/извлечена из кучи) обоими алгоритмами.

### Расчет Ответа
Расстояние не обязательно равно $D_S[V] + D_T[V]$ в точке встречи.
**Формула:** $$Ans = \min_{u \in \text{processed}} (D_S[u] + D_T[u])$$
Нужно перебрать все вершины, которые были затронуты, и найти минимум суммы путей.

### Преимущество
Полезно для графов с большим ветвлением. Если обычная Дейкстра перебирает $C^L$ вершин (экспоненциальный рост), то двусторонняя — примерно $2 \cdot C^{L/2}$, что значительно меньше (корень из исходного количества).

---

## 2. Алгоритм A* (A-star)

**Задача:** Найти кратчайший путь от $S$ до $T$ быстрее, чем Дейкстра, используя знания о природе графа (эвристику).
*Пример:* Граф городов на плоскости. Мы знаем, что путь не может быть короче евклидова расстояния.

### Основные Понятия
*   **$g(v)$** — текущее найденное расстояние от $S$ до $v$ (аналог $d[v]$ в Дейкстре).
*   **$h(v)$** — **эвристика**: оценка расстояния от $v$ до цели $T$.
*   **$f(v) = g(v) + h(v)$** — приоритет вершины в куче. Оценка полной длины пути через $v$.

### Принцип Работы
Это модификация Дейкстры, где вершины извлекаются из очереди приоритетов на основе минимума **$f(v)$**, а не $g(v)$. Это заставляет алгоритм двигаться в сторону цели, а не расширяться равномерно во все стороны.

### Свойства Эвристик

1.  **Допустимая (Admissible) эвристика:**
    *   $h(v) \le dist(v, T)$ для всех $v$. Оценка никогда не превышает реальное расстояние (оценка снизу).
    *   *Свойства:* Гарантирует нахождение правильного ответа, но вершина может раскрываться несколько раз (в худшем случае — экспоненциальное время).
2.  **Монотонная (Monotone) эвристика:**
    *   $h(T) = 0$.
    *   $h(u) \le h(v) + w(u, v)$ (неравенство треугольника).
    *   *Свойства:*
        *   Гарантирует, что каждая вершина раскрывается **не более одного раза**.
        *   Значения $f(v)$ при извлечении не убывают.
        *   Работает так же быстро и корректно, как Дейкстра (вершины обрабатываются в правильном порядке).
    *   *Пример:* Евклидово расстояние на плоскости.

**Вывод:** Если эвристика монотонна — A* оптимален и эффективен. Если только допустима — ответ будет верным, но может работать долго. Если не допустима — корректность не гарантируется (но может работать быстро как жадный алгоритм).

---

## 3. Алгоритм Форда-Беллмана (Отрицательные веса)

**Проблема:** Алгоритм Дейкстры не работает с отрицательными рёбрами.
**Критерий $-\infty$:** Расстояние от $S$ до $T$ равно $-\infty$ тогда и только тогда, когда на пути есть **отрицательный цикл**, достижимый из $S$, из которого можно попасть в $T$.

### Динамическое Программирование (ДП)
Вводим ограничение на количество рёбер в пути.
*   **$dp[k][v]$** — минимальная стоимость пути от $S$ до $v$, использующего **не более $k$** рёбер.

**База:**
*   $dp[0][S] = 0$
*   $dp[0][v] = +\infty$ (для остальных)

**Переход (итерация $k$):**
$$dp[k][v] = \min (dp[k-1][v], \quad \min_{u \to v} (dp[k-1][u] + w(u, v)))$$
То есть мы либо оставляем старый путь, либо пытаемся улучшить его, добавив одно ребро.

### Асимптотика
*   Всего $N$ вершин, значит, кратчайший путь без циклов имеет длину не более $N-1$.
*   Нужно $N-1$ итераций.
*   На каждой итерации перебираем все рёбра $M$.
*   Итоговая сложность: **$O(N \cdot M)$**.

### Работа С Отрицательными Циклами
Если в графе нет отрицательных циклов, то на $N-1$ шаге значения стабилизируются.

**Как найти отрицательные циклы:**
1.  Выполнить **$N$-ю итерацию** алгоритма.
2.  Если для какой-то вершины $v$ произошло улучшение ($dp[N][v] < dp[N-1][v]$), значит, эта вершина либо лежит на отрицательном цикле, либо достижима из него.
3.  Чтобы найти **все** вершины с расстоянием $-\infty$:
    *   Найти все вершины, где произошло улучшение на $N$-м шаге.
    *   Запустить из них обход (DFS/BFS). Все достижимые вершины помечаем как имеющие расстояние $-\infty$.

---

### Итоговое Сравнение Алгоритмов Лекции

| Алгоритм | Условия | Сложность | Особенность |
| :--- | :--- | :--- | :--- |
| **Двусторонний Дейкстра** | $w \ge 0$ | Как Дейкстра (лучше константа) | Встреча двух волн, выигрыш на ветвящихся графах. |
| **A*** | $w \ge 0$, есть $h(v)$ | Зависит от эвристики | Направленный поиск. Эффективность зависит от качества эвристики. |
| **Форд-Беллман** | Любые веса | $O(NM)$ | Работает с отрицательными весами, находит отрицательные циклы. |