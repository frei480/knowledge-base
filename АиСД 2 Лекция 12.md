# Лекция 12: Алгоритм Диница И Блоковые Потоки

## 1. Лемма О Масштабировании (закрытие Долга С Прошлой лекции)
В алгоритме масштабирования емкостей мы идем по битам $k$ от старшего к младшему.
**Утверждение (Лемма 1):** Если мы выполнили $k$-ю итерацию (для масштаба $2^k$), то остаточный поток, который нужно пустить до максимума, не превышает $2^k \cdot |E|$.

**Доказательство:**
1. На $k$-й итерации мы ищем поток в сети, где емкости — это $C_{scaled} = \lfloor C / 2^k \rfloor$.
2. Если найден максимальный поток, то в остаточной сети (с точки зрения масштабированных весов) минимальный разрез равен 0. То есть нет пути из $S$ в $T$ по ребрам с целой частью $\ge 1$.
3. При возвращении к исходным весам (умножении на $2^k$) на этих ребрах разреза могут "возродиться" только остатки от деления на $2^k$.
4. Величина каждого такого остатка строго меньше $2^k$.
5. Суммарная пропускная способность этого разреза меньше $2^k \cdot |E|$.
6. Так как поток ограничен любым разрезом, оставшийся поток также не превышает эту величину.

---

## 2. Алгоритм Диница: Основная Идея
**Проблема Эдмондса-Карпа:** Он ищет один кратчайший путь, насыщает его, перестраивает сеть и ищет заново. Это долго ($O(VE^2)$).
**Идея Диница:** Насыщать **сразу все** кратчайшие пути определенной длины за одну фазу. Это улучшает асимптотику до $O(V^2E)$.

### Основные Определения
1.  **Слоистая сеть (Layered Network):**
    *   Строится запуском BFS из $S$ по остаточной сети.
    *   Вершины разбиваются на слои $V_0, V_1, \dots, V_L$ по расстоянию от $S$ ($S \in V_0, T \in V_L$).
    *   В слоистую сеть включаются **только** те ребра $(u, v)$, для которых $dist(v) = dist(u) + 1$. (Ребра строго "слева направо").
2.  **Блокирующий поток (Blocking Flow):**
    *   Поток в слоистой сети, который нельзя увеличить без введения обратных ребер (или возврата по слоям).
    *   *Свойство:* Любой путь из $S$ в $T$ в слоистой сети содержит хотя бы одно насыщенное ребро.

### Алгоритм
```python
while (существует путь из S в T в остаточной сети):
    1. Построить слоистую сеть (BFS).
    2. Найти блокирующий поток в этой слоистой сети (специальный DFS).
    3. Добавить этот поток к текущему глобальному потоку, обновить остаточную сеть.
```

---

## 3. Поиск Блокирующего Потока
Для поиска используется модифицированный DFS.

**Оптимизация (удаление бесполезных ребер):**
*   Если DFS зашел в вершину $v$ и по ребру $(v, u)$ не смог дойти до $T$, это ребро помечается «бесполезным» («мертвым») и больше не рассматривается в **текущей** фазе.
*   Реализуется с помощью указателя `ptr[v]`, который хранит индекс первого нерассмотренного ребра в списке смежности.

**Процесс:**
*   Запускаем DFS из $S$. Идем только по ребрам слоистой сети.
*   Если дошли до $T$ — проталкиваем найденный минимум (bottleneck) вдоль пути, откатываемся, обновляем остаточные емкости.
*   Если из вершины нельзя пройти дальше (все исходящие ребра пройдены или ведут в тупик), DFS возвращает 0, и мы «отрезаем» вход в эту вершину (сдвигаем `ptr`).

**Асимптотика одной фазы (поиска блокирующего потока):**
*   Каждый сдвиг `ptr` навсегда исключает ребро из рассмотрения в этой фазе ($O(E)$ сдвигов суммарно).
*   Каждое успешное нахождение пути насыщает минимум одно ребро ($O(E)$ насыщений). Длина пути не более $V$.
*   Итоговая сложность фазы: **$O(VE)$**.

---

## 4. Общая Асимптотика Алгоритма Диница
Для доказательства общей сложности нужно оценить количество фаз (итераций `while`).

**Теорема о расстоянии:**
Расстояние между $S$ и $T$ в остаточной сети ($dist(S, T)$) строго возрастает с каждой итерацией алгоритма Диница.
*   *Интуиция:* Мы насыщаем все пути длины $L$. В остаточной сети появляются либо обратные ребра (путь удлиняется), либо ребра внутри слоя. Кратчайший путь не может стать короче или таким же.

**Вывод:**
1.  Максимальное расстояние — $V$.
2.  Значит, количество фаз не превышает $V$.
3.  Сложность фазы $O(VE)$.
4.  **Общая сложность:** **$O(V^2E)$**.

---

## 5. Улучшения Оценок (Теорема Карзанова)
Асимптотику можно оценить точнее, используя понятие потенциала сети.

### Определения
*   $C_{in}(v)$ — сумма входящих пропускных способностей вершины $v$.
*   $C_{out}(v)$ — сумма исходящих.
*   **Потенциал вершины** $P(v) = \min(C_{in}(v), C_{out}(v))$.
*   **Потенциал сети** $P = \sum_{v \neq S, T} P(v)$.

### Первая Теорема Карзанова
Количество итераций в алгоритме Диница есть **$O(\sqrt{P})$**.

**Доказательство (схема):**
1.  **Лемма 1:** $dist(S, T) \le 1 + P/F_{rem}$, где $F_{rem}$ — поток, который осталось пустить. (Доказывается через декомпозицию потока: поток «съедает» потенциал слоев).
2.  **Лемма 2:** Потенциал сети не меняется при переходе к остаточной сети.
3.  **Логика:**
    *   Выполним $\sqrt{P}$ итераций.
    *   Расстояние $L$ станет не меньше $\sqrt{P}$.
    *   Из Леммы 1 следует, что оставшийся поток $F_{rem}$ мал ($O(\sqrt{P})$).
    *   Так как каждая итерация пускает хотя бы 1 единицу потока (для целочисленных сетей), осталось не более $O(\sqrt{P})$ итераций.
    *   Итого: $O(\sqrt{P})$.

---

## 6. Применение В Частных Случаях

### А. Единичные Сети (Unit Networks)
Сети, где все пропускные способности равны 0 или 1.
*   Потенциал $P \le E$ (каждое ребро дает вклад 1).
*   Количество итераций: $O(\sqrt{E})$.
*   Сложность поиска блокирующего потока: $O(E)$ (так как ребра насыщаются и удаляются сразу, каждое ребро просматривается $O(1)$ раз).
*   **Итоговая сложность:** **$O(E\sqrt{E})$**.

### Б. Максимальное Паросочетание (Алгоритм Хопкрофта-Карпа)
Задачу о паросочетании в двудольном графе можно свести к потоку:
*   Добавляем $S$ (ребра ко всем вершинам левой доли, вес 1).
*   Добавляем $T$ (ребра от всех вершин правой доли, вес 1).
*   Ребра между долями ориентируем слева направо (вес $\infty$ или 1).

В такой сети:
*   Для вершин левой доли $C_{in}=1$.
*   Для вершин правой доли $C_{out}=1$.
*   Значит, $P(v) = 1$ для всех вершин.
*   Суммарный потенциал $P = O(V)$.
*   Количество итераций по Карзанову: $O(\sqrt{V})$.
*   Сложность фазы: $O(E)$.

**Итоговая сложность:** **$O(E\sqrt{V})$**.
Это алгоритм Хопкрофта-Карпа — по сути, применение Диница к специфической сети. Это асимптотически лучше, чем алгоритм Куна ($O(VE)$), хотя на практике Кун может быть быстрее из-за константы.