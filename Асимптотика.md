Пусть $f(n), g(n) : \mathbb{N} \to \mathbb{R}_{\geq 0}$, тогда:
- $f(n) = O(g(n))$, если $\exists  C \in \mathbb{R}_{>0}$ и $\exists N \in \mathbb{N} \forall n \in \mathbb{N}_{\geq N}$ : $f(n) ≤ C *g(n)$;
то есть, $f$ ограничена сверху функцией $g$ (с точностью до постоянного множителя) асимптотически
- $f(n) = Ω(g(n))$, если $\exists  c \in \mathbb{R}_{>0}$ и $\exists N \in \mathbb{N} \forall n \in \mathbb{N}_{\geq N}$ : $f(n) ≤ c*g(n)$;
то есть, то есть, ограничена снизу функцией  (с точностью до постоянного множителя) асимптотически
- $f(n) = Θ(g(n))$, если $f(n) = O(g(n))$ и $f(n) = Ω(g(n))$.
то есть время работы $f$ ограничено и сверху и снизу функцией $g$

