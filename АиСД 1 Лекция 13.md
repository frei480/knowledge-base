# Лекция 13: B-деревья (удаление) И Красно-чёрные Деревья

## Часть 1. Удаление (Erase) В B-дереве

В B-дереве, где каждая вершина содержит от $t-1$ до $2t-1$ ключей (кроме корня), удаление ключа $X$ происходит за один проход сверху вниз.

**Главная идея:** Поддерживать инвариант при спуске — в вершине, в которой мы находимся, должно быть **хотя бы $t$ ключей**. Это нужно, чтобы при необходимости можно было удалить ключ или спустить его вниз без нарушения свойств B-дерева.

### Алгоритм Спуска
Пусть мы стоим в вершине и собираемся спуститься в ребёнка (по указателю), но в этом ребёнке всего $t-1$ ключ (минимально допустимое количество). Чтобы выполнить инвариант, нужно пополнить ребёнка **до** входа в него:

1.  **Заимствование у брата (Sibling):**
    *   Если у соседнего брата (левого или правого) есть **хотя бы $t$ ключей**, мы переносим ключ через родителя.
    *   *Механизм:* Берём крайний ключ у брата $\to$ поднимаем его в родителя $\to$ спускаем разделяющий ключ из родителя в нашего ребёнка.
    *   В результате у ребёнка становится $t$ ключей, у брата не меньше $t-1$. Можно спускаться.

2.  **Слияние с братом (Merge):**
    *   Если у всех братьев тоже по $t-1$ ключу, заимствовать нельзя.
    *   *Механизм:* Объединяем текущего ребёнка и его брата. Для склейки «забираем» разделяющий их ключ из родителя и опускаем его между ними.
    *   Новая вершина содержит: $(t-1) + 1 \text{ (из родителя)} + (t-1) = 2t-1$ ключ.
    *   Это разрешено, так как в родителе по инварианту было хотя бы $t$ ключей, значит, после изъятия одного останется хотя бы $t-1$.

### Удаление Ключа X
Когда мы дошли до вершины, содержащей ключ $X$ (и в ней гарантированно $\ge t$ ключей):

1.  **Лист:** Просто удаляем ключ $X$.
2.  **Внутренняя вершина:**
    *   Если в левом поддереве ребёнка $\ge t$ ключей: находим там **максимум**, ставим на место $X$, рекурсивно удаляем этот максимум.
    *   Если в правом поддереве ребёнка $\ge t$ ключей: находим там **минимум**, ставим на место $X$, рекурсивно удаляем.
    *   Если в обоих детях по $t-1$ ключу: **Склеиваем** детей (опуская $X$ вниз). Теперь $X$ находится в новой большой вершине. Запускаем удаление оттуда.

**Сложность:** $O(t \log_t N)$.

---

## Часть 2. Красно-чёрное Дерево (Red-Black Tree)

### Определение И Свойства
Это бинарное дерево поиска, удовлетворяющее 5 свойствам:
1.  **Цвет:** Каждая вершина либо красная (Red), либо чёрная (Black).
2.  **Корень:** Всегда чёрный.
3.  **Листья (Nil):** Все листья — фиктивные (пустые, null), не содержат данных и всегда **чёрные**.
4.  **Красное правило:** У красной вершины оба сына обязательно чёрные (нет двух красных подряд).
5.  **Чёрная высота (Black Height, bh):** На любом пути от данной вершины до любого листа количество чёрных вершин одинаково.

### Оценка Высоты
Лемма: Количество *ключей* в поддереве вершины $v$ с чёрной высотой $bh(v)$ (без учета самой $v$) составляет минимум $2^{bh(v)} - 1$.
*   Поскольку красные вершины не могут идти подряд, реальная высота $h \le 2 \cdot bh$.
*   Следовательно, высота дерева $h \le 2 \log_2(N+1)$. Операции работают за логарифмическое время.

---

## Часть 3. Вставка (Insert)

Новый элемент $X$ вставляется как в обычное BST и красится в **Красный** цвет (это не нарушает чёрную высоту, но может нарушить правило "нет двух красных подряд").

**Исправление нарушений (если $X$ и его родитель $P$ красные):**
Рассматриваем дедушку $G$ и дядю $U$ (брата родителя).

1.  **Случай 1: Дядя $U$ — Красный.**
    *   *Действие:* Перекраска. $P$ и $U$ становятся чёрными, $G$ становится красным.
    *   *Проблема:* $G$ может стать красным при красном родителе. Проблема переносится вверх (рекурсивно).

2.  **Случай 2: Дядя $U$ — Чёрный (или Nil).**
    *   Здесь нужны повороты.
    *   *Случай 2.1 (Line / Zig-Zig):* $X$ и $P$ находятся по одну сторону (оба левые или оба правые дети).
        *   Делаем поворот в $G$. $P$ становится корнем поддерева (чёрным), $G$ и $X$ — его красными детьми.
    *   *Случай 2.2 (Triangle / Zig-Zag):* $X$ и $P$ по разные стороны (например, $P$ левый сын, $X$ правый).
        *   Поворот в $P$, сводим к Случаю 2.1, затем поворот в $G$.
    *   *Итог:* Максимум 2 поворота на вставку. Проблема решается окончательно.

---

## Часть 4. Удаление (Erase) В КЧ-дереве

Самая сложная часть.
Сначала выполняем стандартное удаление для BST: если у вершины 2 обычных сына, меняем её с минимумом в правом поддереве (у которого $\le 1$ обычного сына).
Сводим задачу к удалению вершины $v$, у которой **максимум один обычный сын**.

**Базовые случаи:**
1.  **Удаляемая вершина Красная:** Просто удаляем. Свойства не нарушаются (чёрная высота не меняется, подряд красных не возникает).
2.  **Удаляемая вершина Чёрная, есть Красный сын:** Удаляем чёрную, на её место ставим красного сына и перекрашиваем его в **Чёрный**. Свойства сохранены.
3.  **Удаляемая вершина Чёрная и не имеет детей (или сын тоже чёрный/фиктивный):**
    *   При удалении чёрная высота на этом пути уменьшается на 1 ($bh-1$).
    *   Возникает "двойная чернота" или дефицит чёрной высоты в узле. Нужно восстановить баланс.

### Восстановление После Удаления Чёрной Вершины
Пусть $X$ — место, где не хватает чёрной высоты (корень поддерева с $bh-1$). Смотрим на **Брата (Sibling, $S$)** вершины $X$ и Родителя ($P$).
*Примечание: Разбираем случай, когда $X$ слева, $S$ справа. Симметричный случай аналогичен.*

**Случай 1: Брат $S$ — Красный.**
*   Родитель $P$ обязан быть чёрным.
*   Делаем поворот влево вокруг $P$. $S$ становится родителем (чёрным), $P$ становится левым сыном (красным).
*   Теперь у $X$ новый брат (бывший сын $S$), который точно чёрный. Сводим к случаям ниже.

**Случай 2: Брат $S$ — Чёрный.**
Анализируем детей брата ($Nephews$).

*   **Случай 2.1: У брата $S$ есть Красный сын (хотя бы один).**
    *   *2.1.1 (Right-Right):* Правый сын брата ($R$) красный.
        *   Поворот влево вокруг $P$.
        *   Цвета: $S$ получает цвет $P$, $P$ становится чёрным, $R$ становится чёрным.
        *   Дефицит устранён (добавили чёрный узел на путь $X$).
    *   *2.1.2 (Right-Left):* Левый сын брата ($L$) красный, правый чёрный.
        *   Поворот вправо вокруг $S$. $L$ встает на место $S$. Сводим к 2.1.1.
    *   *Итог:* Максимум 3 поворота, рекурсия не нужна.

*   **Случай 2.2: У брата $S$ оба сына Чёрные.**
    *   Перекрашиваем $S$ в **Красный**. Теперь справа тоже высота уменьшилась на 1.
    *   Если родитель $P$ был **Красным**: перекрашиваем $P$ в Чёрный. Баланс восстановлен (справа уменьшилась из-за $S$, но увеличилась из-за $P$, слева увеличилась из-за $P$). Всё, конец.
    *   Если родитель $P$ был **Чёрным**: Теперь у всего поддерева $P$ высота уменьшилась на 1. Запускаем алгоритм **рекурсивно** от $P$ (переносим проблему вверх).

**Итог удаления:** Требуется $O(\log N)$ перекрасок (подъем вверх), но **максимум 3 поворота**.

---

## Часть 5. Сравнение Структур (AVL Vs RB Vs B-Tree)

1.  **B-дерево:** Идеально для дисковых операций (HDD/SSD), так как минимизирует количество доступов к памяти (большой ветвление, локальность данных).
2.  **AVL-дерево:**
    *   Строго сбалансировано ($|h_L - h_R| \le 1$). Высота $\approx 1.44 \log N$.
    *   **Плюс:** Быстрый поиск (`Find`), так как дерево ниже.
    *   **Минус:** Медленная вставка/удаление из-за частых балансировок (удаление может потребовать $O(\log N)$ поворотов).
3.  **Красно-чёрное дерево:**
    *   Слабая балансировка (высота $\le 2 \log N$).
    *   **Плюс:** Очень быстрые вставка и удаление. Гарантированно мало поворотов (макс. 2 на вставку, макс. 3 на удаление). Перекраска — дешевая операция (битфлип).
    *   **Применение:** Стандартные библиотеки (C++ `std::map`, `std::set`, Java `TreeMap`) используют именно RB-деревья, так как они универсальнее при частых изменениях данных.

**Вывод:**
*   Много читаем, редко пишем $\to$ **AVL**.
*   Много пишем и удаляем $\to$ **Красно-чёрное**.
*   Работаем с диском/базами данных $\to$ **B-дерево**.