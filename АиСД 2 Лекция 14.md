# Лекция 14: Метод Потенциалов И Алгоритмы На Деревьях

## 1. Метод Потенциалов Для MinCost MaxFlow
Использование алгоритма Дейкстры для поиска кратчайшего пути в графе с отрицательными весами (но без отрицательных циклов) для ускорения алгоритма MinCost MaxFlow.

### Проблема
Алгоритм МинКост ищет кратчайшие пути в остаточной сети. Изначально отрицательных ребер нет, но после пуска потока появляются обратные ребра с отрицательной стоимостью ($-Cost$). Алгоритм Дейкстры с ними не работает, а Форд-Беллман слишком медленный ($O(VE)$).

### Решение: Пересчет Потенциалов
Вводим потенциал вершины $\phi(v)$. Пересчитываем веса ребер по формуле:
$$Cost_{new}(u, v) = Cost(u, v) + \phi(u) - \phi(v)$$
Согласно неравенству треугольника, если $\phi(v)$ — это кратчайшее расстояние от $S$ до $v$, то новые веса $Cost_{new} \ge 0$. Это позволяет использовать Дейкстру.

**Алгоритм:**
1.  **Первый шаг:** Запускаем Форда-Беллмана (один раз), получаем начальные расстояния. Присваиваем $\phi(v) = dist(v)$.
2.  **Итерации:**
    *   Применяем потенциал: меняем веса ребер на неотрицательные.
    *   Ищем кратчайший путь Дейкстрой (в редуцированном графе путь имеет длину 0).
    *   Пускаем единицу потока. Появляются обратные ребра.
    *   **Важно:** Свойство $\phi = dist$ нарушается. Нужно обновить потенциалы.
    *   Новые потенциалы вычисляются как $\phi_{new}(v) = \phi_{old}(v) + dist_{Dijkstra}(v)$.
    *   Повторяем процесс.

**Асимптотика:** $O(VE + k \cdot E \log V)$ или $O(k \cdot V^2)$, где $k$ — величина потока.

---

## 2. Деревья. Базовые Определения
*   **Дерево:** Связный неориентированный граф без циклов. $|E| = |V| - 1$. Между любой парой вершин есть ровно один простой путь.
*   **Корневое дерево:** Ориентированный граф, полученный из обычного дерева выбором корня и запуском DFS (ориентация ребер от корня). В каждую вершину (кроме корня) входит ровно одно ребро.

---

## 3. LCA (Lowest Common Ancestor) — Наименьший Общий Предок
Задача: для вершин $U$ и $V$ найти такого предка, который является предком обоих и находится максимально глубоко (далеко от корня).

**Базовая проверка на предка:**
Вершина $U$ является предком $V$, если:
$$tin[U] \le tin[V] \quad \text{и} \quad tout[U] \ge tout[V]$$
где $tin/tout$ — времена входа и выхода при DFS. Проверка выполняется за $O(1)$.

### Способ 1: Двоичные Подъемы (Binary Lifting)
*   **Предподсчет:** $up[v][k]$ — предок вершины $v$ на расстоянии $2^k$.
    *   $up[v][0] = parent[v]$
    *   $up[v][k] = up[\ up[v][k-1]\ ][k-1]$
*   **Запрос LCA(u, v):**
    1.  Если $u$ предок $v$, вернуть $u$ (или наоборот).
    2.  Иначе поднимаем $u$ максимально высоко, но так, чтобы он **не стал** предком $v$.
    3.  Идем циклом по $k$ от $\log N$ до 0: если $2^k$-й предок $u$ всё ещё не предок $v$, прыгаем туда.
    4.  В конце $u$ будет ребенком LCA. Ответ: $parent[u]$.
*   **Асимптотика:** Предподсчет $O(N \log N)$, Запрос $O(\log N)$.

### Способ 2: Эйлеров Обход + Sparse Table (RMQ)
*   **Идея:** Выписываем вершины в порядке обхода DFS (вход, спуск, возврат). Получаем массив длиной $2N-1$.
*   LCA($u, v$) — это вершина с **минимальной глубиной** на отрезке между первым вхождением $u$ и первым вхождением $v$ в этом массиве.
*   Задача сводится к RMQ (Range Minimum Query).
*   Используем Sparse Table (разреженную таблицу).
*   **Асимптотика:** Предподсчет $O(N \log N)$, Запрос $O(1)$.

### Способ 3: Алгоритм Фарах-Колтона И Бендера (Линейный LCA)
Позволяет достичь $O(N)$ на прекальк и $O(1)$ на запрос.
*   Сводим задачу к RMQ на массиве глубин Эйлерова обхода.
*   **Свойство:** Соседние элементы в массиве глубин отличаются ровно на $\pm 1$.
*   **Метод блоков:**
    1.  Делим массив на блоки длиной $K = \frac{1}{2} \log N$. Всего блоков $N/K$.
    2.  Между блоками строим Sparse Table. Размер $O(\frac{N}{\log N} \log \frac{N}{\log N}) = O(N)$.
    3.  Внутри блоков: Блок определяется "маской" изменений ($\pm 1$). Всего масок $2^{K-1} = \sqrt{N}$.
    4.  Для всех типов масок предподсчитываем ответы для всех подрезок (Four Russians technique).
*   **Запрос:** Комбинируем минимум внутри крайних блоков (по маске) и минимум между блоками (по Sparse Table).
*   **Итог:** Предподсчет $O(N)$, Запрос $O(1)$.

---

## 4. Решение RMQ Через LCA (Обратное сведение)
Можно решать задачу RMQ (минимум на отрезке) за $O(N)$ / $O(1)$, сведя её к LCA.
1.  Строим **Декартово дерево** (Treap) по массиву за $O(N)$, где ключи — индексы, а приоритеты — значения в массиве ($A[i]$).
2.  В Декартовом дереве предок имеет приоритет меньше, чем дети (свойство кучи).
3.  **Утверждение:** $\min(A[L \dots R])$ соответствует приоритету вершины $LCA(L, R)$ в Декартовом дереве.
4.  Используем алгоритм Фарах-Колтона и Бендера для LCA.

---

## 5. Изоморфизм Деревьев
Два графа изоморфны, если существует биекция вершин, сохраняющая смежность.
Для произвольных графов задача сложная. Для деревьев решается эффективно.

### Изоморфизм Корневых Деревьев
Нужно проверить, совпадают ли структуры поддеревьев.
1.  **Кодирование поддеревьев:** Для каждой вершины вычисляем кортеж (мультимножество) из идентификаторов классов эквивалентности её детей.
2.  **Алгоритм:**
    *   Идем DFS'ом снизу вверх (post-order).
    *   Листу присваиваем класс 0.
    *   Для внутренней вершины собираем классы детей в список, сортируем его.
    *   Этот отсортированный вектор мапим в целое число (идентификатор класса). Если такой вектор уже видели — берем старый ID, иначе выдаем новый.
3.  Если классы корней совпали — деревья изоморфны.
4.  **Асимптотика:** $O(N \log N)$ (из-за сортировки детей и обращений к `map`).

### Изоморфизм Некорневых (обычных) Деревьев
У некорневых деревьев нет фиксированного корня, начало неизвестно.
**Решение через Центроид:**
*   **Центроид** — вершина, при удалении которой размер каждой компоненты связности не превышает $N/2$.
*   В любом дереве есть **1 или 2** центроида.
*   Центроид — инвариант, не зависящий от нумерации вершин.
*   **Алгоритм:** Найти центроиды в обоих деревьях, подвесить деревья за них (рассмотреть 1-2 варианта) и проверить изоморфизм как для корневых деревьев.
## Общий Итог Лекции

* Потенциалы позволяют применять Дейкстру в min-cost flow.
* LCA — ключевая операция для задач на деревьях.
* Существует 3 классических способа поиска LCA:
  1. двоичные подъёмы,
  2. Эйлеров обход + RMQ,
  3. Фарах–Колтон–Бендер (оптимальный).
* RMQ можно свести к LCA и получить оптимальные оценки.
