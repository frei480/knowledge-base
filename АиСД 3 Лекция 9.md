# Лекция 9: Теоретико-числовые Алгоритмы

**Основные темы:**
1. Извлечение квадратного корня в поле $\mathbb{Z}_p$ (Алгоритм Тоннелли — Шенкса).
2. Вероятностные тесты на простоту (Тест Ферма и Тест Миллера — Рабина).
3. Алгоритм факторизации числа ($\rho$-алгоритм Полларда).

---

## 1. Извлечение Квадратного Корня В $\mathbb{Z}_p$

**Задача:** Решить сравнение $x^2 \equiv a \pmod p$, где $p$ — нечетное простое число, $a \neq 0$.

### Предварительная Проверка (Критерий Эйлера)
Согласно малой теореме Ферма, $a^{p-1} \equiv 1 \pmod p$.
Возведем $a$ в степень $(p-1)/2$:
*   Если $a^{(p-1)/2} \equiv -1 \pmod p$, то $a$ — **квадратичный невычет** (решений нет).
*   Если $a^{(p-1)/2} \equiv 1 \pmod p$, то $a$ — **квадратичный вычет** (решения есть).

*Алгоритм (общая идея):* Сначала проверяем условие разрешимости. Если решения есть, ищем их.

### Частный Случай: $p \equiv 3 \pmod 4$
Если $p$ имеет вид $4k+3$, то $p-1 = 4k+2 = 2(2k+1)$. Степень двойки $S=1$.
В этом случае решение выписывается явной формулой:
$$ x = \pm a^{\frac{p+1}{4}} $$
*Проверка:*
$x^2 \equiv a^{\frac{p+1}{2}} \equiv a^{\frac{p-1}{2} + 1} \equiv a^{\frac{p-1}{2}} \cdot a \pmod p$.
Так как $a$ — вычет, то $a^{\frac{p-1}{2}} \equiv 1$, следовательно $x^2 \equiv a$.

### Общий Случай: Алгоритм Тоннелли — Шенкса
Если $p$ нельзя записать как $4k+3$, используется общий итеративный алгоритм.

**1. Подготовка:**
Представим $p-1$ в виде $Q \cdot 2^S$, где $Q$ — нечетное.
Находим (случайным перебором) любой квадратичный невычет $b$ (число, для которого $b^{(p-1)/2} \equiv -1$). Это делается быстро, так как невычетов ровно половина.

**2. Инициализация переменных:**
Лектор вводит переменные $u_0, v_0$ и константу $C$:
*   $M = Q$ (нечетная часть $p-1$).
*   $C = b^M \pmod p$ (константа, порядок которой — степень двойки).
*   $u_0 = a^M \pmod p$.
*   $v_0 = a^{\frac{M+1}{2}} \pmod p$.

Выполняется соотношение: $v_0^2 \equiv u_0 \cdot a \pmod p$.
*   Если $u_0 \equiv 1$, то решение найдено: $x = \pm v_0$.
*   Иначе нужно «сводить» $u_k$ к 1, поддерживая инвариант $v_k^2 = u_k \cdot a$.

**3. Итеративный процесс:**
Мы знаем, что порядок $u_k$ является степенью двойки (так как $u_k^{2^S} \equiv 1$).
Пусть порядок $u_k$ равен $2^R$ (где $0 < R < S$).
Мы хотим перейти к паре $(u_{k+1}, v_{k+1})$, где порядок $u_{k+1}$ меньше, чем у $u_k$.

*   Находим $R$ прямым возведением $u_k$ в степени $2^1, 2^2 \dots$ пока не получим 1.
*   Домножаем $u_k$ на подходящую степень числа $C$, чтобы «убить» множитель, мешающий стать единицей.
*   Формулы перехода:
    $$ u_{k+1} = u_k \cdot C^{2^{S-R}} $$
    $$ v_{k+1} = v_k \cdot C^{2^{S-R-1}} $$
    (Показатели степеней корректируются, чтобы сохранить инвариант).

Каждый шаг уменьшает степень двойки в порядке элемента $u$. Максимум за $S$ шагов (где $S \le \log p$) мы получим $u=1$.

**Сложность:** $O(\log^2 p)$ (из-за бинарных возведений в степень и поиска порядка).

---

## 2. Проверка На Простоту

### Тест Ферма
Основан на малой теореме Ферма: Если $N$ простое, то для любого $a$ ($1 \le a < N$):
$$ a^{N-1} \equiv 1 \pmod N $$
**Проблема:** Существуют составные числа (числа Кармайкла), для которых это условие выполняется для всех $a$, взаимно простых с $N$. Поэтому тест Ферма ненадежен.

### Тест Миллера — Рабина
Улучшение теста Ферма.
Пусть $N-1 = m \cdot 2^s$, где $m$ нечетно.

**Алгоритм для одного раунда (с числом $a$):**
1. Строим последовательность:
   $$ t_0 = a^m, \quad t_1 = t_0^2, \quad t_2 = t_1^2, \dots, \quad t_s = t_{s-1}^2 = a^{N-1} $$
2. Если $t_s \neq 1$, то $N$ — **составное** (по теореме Ферма).
3. Если $t_s = 1$, смотрим на предыдущие элементы.
   В поле вычетов по простому модулю уравнение $x^2 \equiv 1$ имеет только два корня: $1$ и $-1$ (он же $N-1$).
   *   Поэтому, если в последовательности первая встречающаяся единица стоит на позиции $k > 0$, то перед ней ($t_{k-1}$) должно стоять число $-1$.
   *   Если перед единицей стоит число, отличное от $-1$ и $1$, то $N$ — **составное**.

**Определение «Свидетеля» (Witness):**
Число $a$ называется свидетелем простоты (точнее, свидетелем составности), если тест с числом $a$ доказывает, что $N$ составное. Это происходит в двух случаях:
1. $a^{N-1} \not\equiv 1 \pmod N$.
2. В последовательности возведений в квадрат появилась единица, которой предшествовало не $-1$.

**Теорема о количестве свидетелей:**
Если $N$ — составное нечетное число, то **как минимум половина** чисел $a \in [1, N-1]$ являются свидетелями составности.
*Доказательство (схема):*
Строится инъективное отображение из множества «несвидетелей» в множество свидетелей.
1. *Случай $N = p^k$ (степень простого):* Берется число $W = 1 + p^{k-1}$. Доказывается, что оно свидетель.
2. *Случай $N = P \cdot Q$ (разные простые множители):*
   *   Рассматриваются несвидетели. Среди них выбирается тот ($H$), у которого $-1$ появляется в последовательности степеней максимально поздно (на позиции $j$).
   *   Конструируется специальное число $W$, которое по модулю одной части делителей $N$ ведет себя как $H$, а по модулю другой — как $1$.
   *   Показывается, что $W$ — свидетель, и что умножение на $W$ переводит любой несвидетель в уникальный свидетель.

**Итог:** Запустив тест $k$ раз с разными случайными $a$, вероятность ошибки (признать составное число простым) составляет $\le 2^{-k}$.
Сложность: полиномиальная от $\log N$ (примерно $O(k \log^3 N)$).

---

## 3. Факторизация (Разложение На множители)

**Задача:** Дано составное $N$, найти нетривиальный делитель.

*   *Наивный метод:* Перебор делителей до $\sqrt{N}$. Сложность $O(\sqrt{N})$.
*   *Цель:* Алгоритм со сложностью $O(N^{1/4})$.

### $\rho$-алгоритм Полларда (Pollard's Rho algorithm)
Эвристический алгоритм, основанный на парадоксе дней рождения.

**Идея:**
1. Если $N$ составное, у него есть простой делитель $p \le \sqrt{N}$.
2. Мы хотим найти два числа $x$ и $y$, которые различны по модулю $N$ ($x \not\equiv y \pmod N$), но совпадают по модулю $p$ ($x \equiv y \pmod p$).
3. Если такие $x, y$ найдены, то разность $|x-y|$ делится на $p$, но не делится на $N$.
4. Тогда $d = \text{gcd}(|x-y|, N)$ будет нетривиальным делителем $N$ (равным $p$ или кратным $p$).

**Генерация последовательности:**
Используем псевдослучайную функцию, например:
$$ f(x) = (x^2 + c) \pmod N $$
Строим последовательность $x_0, x_1 = f(x_0), x_2 = f(x_1), \dots$.

**Почему это работает:**
*   Рассмотрим эту последовательность по модулю $p$ (неизвестного нам делителя).
*   Так как значений по модулю $p$ всего $p$, последовательность зациклится.
*   Согласно парадоксу дней рождения, коллизия (повтор значения по модулю $p$) произойдет в среднем через $\sqrt{p}$ шагов.
*   Так как $p \le \sqrt{N}$, то число шагов до коллизии $\approx \sqrt{\sqrt{N}} = N^{1/4}$.

**Графическая интерпретация:**
Последовательность выглядит как буква $\rho$ (rho): «хвост», переходящий в цикл.

**Алгоритм (поиск коллизии):**
На лекции детально алгоритм поиска цикла (Флойда «черепаха и заяц») не расписан до конца, но обозначена суть: мы ищем пару $x_i, x_j$, вычисляем $\text{gcd}(|x_i - x_j|, N)$ и надеемся получить делитель. Если $\text{gcd} = 1$, продолжаем; если $\text{gcd} = N$, алгоритм не сработал (нужно сменить $c$ или $x_0$).

**Итоговая сложность:** Ожидаемое время $O(N^{1/4} \log N)$.