# Лекция 11: Потоки В Сетях. Часть 1: Основы И Алгоритмы

## 1. Введение И Постановка Задачи
**Мотивация:** Представьте распределенную сеть серверов компании. Нужно быстро передавать данные (ресурс) из одной точки в другую, используя существующие каналы связи. У каждого канала есть ограничение на скорость/объем передачи.
**Задача:** Найти способ передать максимальное количество ресурса от источника к стоку.

## 2. Основные Определения

### Транспортная Сеть
Это ориентированный граф $G = (V, E)$, в котором:
*   $S$ — источник (Source).
*   $T$ — сток (Sink, Target).
*   $C(u, v) \ge 0$ — **пропускная способность** (capacity) ребра. Если ребра нет, $C=0$.

### Поток (Flow)
Функция $f(u, v)$, сопоставляющая каждой паре вершин целое число, со следующими свойствами:
1.  **Ограничение пропускной способности:** $f(u, v) \le C(u, v)$.
2.  **Антисимметричность:** $f(u, v) = -f(v, u)$.
    *   *Смысл:* Если мы передали $X$ единиц от $u$ к $v$, мы считаем, что от $v$ к $u$ передано $-X$. Это удобно для математической записи и работы с «отменой» потока.
3.  **Сохранение потока:** Для любой вершины $u$, кроме $S$ и $T$, сумма входящих и исходящих потоков равна нулю (ничего не накапливается). С учетом антисимметричности:
    $$ \sum_{v \in V} f(u, v) = 0 $$

**Величина потока ($|f|$):** Суммарный поток, вытекающий из истока $S$:
$$ |f| = \sum_{v \in V} f(s, v) $$

## 3. Остаточная Сеть ($G_f$)
Сеть, показывающая, сколько еще можно передать по каждому ребру с учетом уже пущенного потока.
*   **Остаточная пропускная способность:** $C_f(u, v) = C(u, v) - f(u, v)$.
*   В $G_f$ включаются только ребра с $C_f > 0$.
*   Благодаря антисимметричности, если по ребру $(u, v)$ течет поток $k$, то появляется обратное ребро $(v, u)$ с остаточной пропускной способностью $k$ (возможность «отменить» поток).

## 4. Разрезы (Cuts)
**Разрез $(S_{cut}, T_{cut})$** — разбиение вершин графа на два непересекающихся множества, где $s \in S_{cut}$ и $t \in T_{cut}$.

*   **Величина разреза:** Сумма пропускных способностей ребер, идущих из $S_{cut}$ в $T_{cut}$:
    $$ C(S_{cut}, T_{cut}) = \sum_{u \in S_{cut}, v \in T_{cut}} C(u, v) $$
*   **Поток через разрез:** Равен величине потока в сети ($|f|$).
*   **Лемма:** Величина любого потока не превышает величины любого разреза ($|f| \le C_{cut}$).

## 5. Теорема Форда-Фалкерсона (Критерий максимальности)
Следующие три утверждения эквивалентны:
1.  Поток $f$ максимален.
2.  В остаточной сети $G_f$ нет путей из $S$ в $T$.
3.  Существует разрез, величина которого равна величине потока.

**Доказательство (схема):**
*   $(1 \to 2)$: Если бы путь был, поток можно было бы увеличить.
*   $(2 \to 3)$: Если пути нет, построим разрез: $S_{cut}$ — все вершины, достижимые из $S$ в $G_f$, $T_{cut}$ — остальные. Ребра из $S_{cut}$ в $T_{cut}$ насыщены ($f = C$), значит величина потока равна величине этого разреза.
*   $(3 \to 1)$: Так как поток не может быть больше разреза, а мы нашли равный, значит, больше нельзя $\to$ максимум.

## 6. Алгоритм Форда-Фалкерсона
**Идея:** Пока в остаточной сети есть путь из $S$ в $T$ (увеличивающий путь), пускаем по нему поток.
1.  Ищем любой путь из $S$ в $T$ в $G_f$ (например, с помощью DFS).
2.  Находим «узкое место» на пути — минимальную остаточную пропускную способность $\Delta$.
3.  Увеличиваем поток вдоль пути на $\Delta$ (не забывая добавлять $-\Delta$ к обратным ребрам).
4.  Повторяем, пока пути существуют.

**Проблема:** Если выбирать пути неудачно (например, DFS находит длинные зигзаги), а пропускные способности большие, алгоритм работает очень долго.
*   *Асимптотика:* $O(|f| \cdot E)$, где $|f|$ — величина макс. потока.

## 7. Алгоритм Эдмондса-Карпа
Модификация метода Форда-Фалкерсона для гарантии полиномиального времени работы.
**Идея:** Искать в остаточной сети **кратчайший** путь (по количеству ребер) с помощью **BFS**.

**Анализ сложности:**
1.  **Лемма 1:** Расстояние от $S$ до любой вершины $v$ в остаточной сети не уменьшается в процессе работы алгоритма.
2.  **Лемма 2:** Каждое ребро может стать «критическим» (насыщенным, исчезнуть из $G_f$) не более $O(V)$ раз (точнее, $V/2$).
    *   Чтобы ребро снова стало ненасыщенным, нужно пустить поток по обратному ребру, а это возможно только если расстояние до вершин увеличилось.

**Итоговая асимптотика:**
*   Всего насыщений ребер: $O(V \cdot E)$.
*   Каждая итерация BFS насыщает минимум одно ребро.
*   Сложность одного BFS: $O(E)$.
*   **Итого:** $O(V \cdot E^2)$.

## 8. Техника Масштабирования (Scaling)
Метод улучшения асимптотики для больших пропускных способностей.
**Идея:** Пускать поток не по 1 единице, а большими порциями (битами числа), переходя от старших битов к младшим.

**Алгоритм:**
1.  Пусть $C_{max}$ — максимальная пропускная способность. Перебираем $k$ от $\lfloor \log_2 C_{max} \rfloor$ до $0$.
2.  Рассматриваем только ребра с пропускной способностью $\ge 2^k$.
3.  Ищем пути в таком урезанном графе, насыщаем их.
4.  Переходим к $k-1$ (уточняем поток).

**Асимптотика:** $O(E^2 \log C_{max})$.
*   Это выгоднее Эдмондса-Карпа, если пропускные способности очень велики, но граф разреженный.