# Лекция 14: Хеш-таблицы

## 1. Введение И Сравнение С Деревьями Поиска

**Предыстория:**
Ранее рассматривались деревья поиска. Они поддерживают операции добавления, удаления и поиска, а также позволяют выводить элементы в отсортированном порядке и выполнять операции диапазона (например, `LowerBound`).

**Хеш-таблицы:**
*   **Цель:** Реализовать тот же функционал (добавление, удаление, проверка наличия), но быстрее.
*   **Отличия от деревьев:**
    *   *Минус:* Теряется упорядоченность элементов. Нельзя быстро вывести все элементы по возрастанию или найти ближайший больший/меньший.
    *   *Плюс:* Асимптотика операций улучшается с логарифмической $O(\log N)$ до константной $O(1)$ (в среднем).

## 2. Основная Идея Хеширования

**Проблема:**
Есть универсум $U$ (множество всех возможных ключей) и мы хотим хранить подмножество $S$.
*   Если $U$ маленькое (например, числа от 0 до $K$), можно завести массив `bool` размером $K$ (прямая адресация).
*   Если $U$ большое (произвольные числа), создать массив размером $U$ невозможно по памяти.

**Решение:**
Отобразить большой универсум $U$ в меньшее множество чисел $[0 \dots M-1]$ с помощью **хеш-функции** $H(x)$.
*   $M$ — размер хеш-таблицы (массива).
*   В идеале $M \approx N$ (количество хранимых элементов).

**Коллизии:**
Так как $|U| \gg M$, неизбежны ситуации, когда $H(x) = H(y)$ для $x \neq y$. Это называется **коллизией**.
Задача хеширования — минимизировать коллизии, «размазывая» ключи по таблице равномерно.

**Случайная хеш-функция (Идеальное хеширование):**
Если бы $H(x)$ была абсолютно случайной функцией, то для любых двух фиксированных ключей $x, y$:
$$P(H(x) = H(y)) = \frac{1}{M}$$
Это обеспечивает равномерное распределение.

## 3. Разрешение Коллизий: Метод Цепочек (Chaining)

Самый простой способ борьбы с коллизиями.

**Структура:**
*   Массив списков (векторов) размером $M$: `List L[0…M-1]`.
*   Все элементы с хешем $i$ хранятся в списке $L[i]$.

**Операции:**
1.  **Insert(x):**
    *   Вычисляем $i = H(x)$.
    *   Добавляем $x$ в начало списка $L[i]$ (добавление в начало быстрее, $O(1)$, плюс обеспечивает быстрый доступ к недавно добавленным элементам).
    *   *Замечание:* Если нужно гарантировать отсутствие дубликатов, сначала делается Find.
2.  **Delete(x):**
    *   Вычисляем $i = H(x)$.
    *   Ищем $x$ в списке $L[i]$ и удаляем его (требует прохода по списку).
3.  **Find(x):**
    *   Вычисляем $i = H(x)$.
    *   Пробегаем по списку $L[i]$.

**Анализ сложности:**
*   Время работы зависит от длины списка.
*   Если $N$ — число элементов, $M$ — число списков.
*   Средняя длина списка (Load Factor) $\alpha = \frac{N}{M}$.
*   Мат. ожидание времени работы: $O(1 + \alpha)$.
*   Если поддерживать $\alpha \approx \text{const}$, то время работы $O(1)$.

## 4. Универсальное Хеширование

**Проблема хранения случайной функции:**
Хранить истинно случайную функцию невозможно (нужен массив размером с $U$).
Решение: использовать **универсальное семейство хеш-функций**.

**Определение:**
Семейство функций $\mathcal{H}$ называется универсальным, если для любых $x \neq y$ вероятность коллизии при выборе случайной функции $h \in \mathcal{H}$ мала:
$$P_{h \in \mathcal{H}}(h(x) = h(y)) \le \frac{1}{M}$$
*(В лекции допускается слабая универсальность с константой, например, $2/M$).*

**Конструкция (для чисел):**
Пусть универсум $U = \mathbb{Z}_p$ (где $p$ — простое число, $p \ge |U|$).
Семейство функций задается параметрами $a, b \in \mathbb{Z}_p$:
$$H_{a,b}(x) = ((ax + b) \mod p) \mod M$$
Где $a, b$ выбираются случайно, $a \neq 0$.

**Доказательство универсальности (идея):**
1.  Рассматривается отображение пары $(x, y) \to (r, s)$, где $r = ax+b, s = ay+b \pmod p$.
2.  Это система линейных уравнений. Так как $x \neq y$, матрица системы невырождена.
3.  Существует взаимно-однозначное соответствие между парами $(a, b)$ и $(r, s)$.
4.  Поскольку $(a, b)$ выбираются случайно и равномерно, то и $(r, s)$ распределены равномерно в $\mathbb{Z}_p^2$.
5.  Вероятность коллизии сводится к вероятности того, что два случайных числа $r$ и $s$ дадут одинаковый остаток по модулю $M$. Эта вероятность оценивается как $\approx 1/M$.

## 5. Итоговый Алгоритм И Рехеширование (Rehashing)

**Алгоритм:**
1.  Выбираем простое $p > U$.
2.  Выбираем начальный размер таблицы $M$ (например, 16).
3.  Выбираем случайные $a, b$ для хеш-функции.
4.  Выполняем операции.
5.  Поддерживаем **Load Factor** $\alpha = N/M$.

**Rehashing:**
*   Если $\alpha$ превышает порог (например, $0.75$), таблица считается переполненной (списки слишком длинные).
*   **Действие:**
    1.  Увеличиваем $M$ в 2 раза.
    2.  Выбираем новую хеш-функцию (новые $a, b$).
    3.  Перекладываем все элементы из старой таблицы в новую (пересчитывая хеши).
*   **Сложность:** Перестроение занимает $O(N)$, но происходит редко (экспоненциально). Амортизированная стоимость добавления остается $O(1)$.

**Вывод:**
Хеш-таблица с цепочками и универсальным хешированием работает за **$O(1)$ в среднем**.

---

## 6. Совершенное Хеширование (Perfect Hashing)

**Задача:**
Дано **фиксированное** (статическое) множество ключей $S$ заранее.
Нужно построить структуру данных, которая отвечает на запросы `Find` за **чистое $O(1)$** в худшем случае (без вероятностей и амортизации при запросе).

**Идея (Парадокс дней рождения):**
*   Если кидать $N$ шаров в $M$ корзин, и $M \approx N^2$, то вероятность коллизии очень мала (< 1/2).
*   Если $M \approx N$, коллизии почти гарантированы.

**Метод (Двухуровневое хеширование):**

1.  **Первый уровень:**
    *   Выбираем внешнюю хеш-функцию $H_{out}$ с размером таблицы $M = N$.
    *   Ключи распределяются по корзинам. Пусть $c_i$ — количество элементов, попавших в $i$-ю корзину.
    *   **Критерий успеха:** Сумма квадратов размеров корзин должна быть линейной: $\sum c_i^2 \le 4N$.
    *   По теореме, вероятность этого события > 1/2. Если условие не выполнилось — выбираем новую $H_{out}$ и пробуем снова (в среднем 2 попытки).

2.  **Второй уровень:**
    *   Для каждой $i$-й корзины, в которой оказалось $c_i$ элементов, строим свою **внутреннюю хеш-таблицу**.
    *   Размер внутренней таблицы выбираем равным **$m_i = c_i^2$**.
    *   Благодаря квадратичному размеру, можно легко подобрать такую внутреннюю хеш-функцию $h_i$, что **коллизий не будет вообще**.
    *   Вероятность найти такую функцию > 1/2. Просто пробуем случайные $h_i$, пока не получится без коллизий.

**Память:**
Суммарный размер памяти = размер внешней таблицы + размеры внутренних таблиц.
$Size \approx N + \sum c_i^2$.
Так как мы гарантировали $\sum c_i^2 \le 4N$, то общая память $O(N)$ (линейная).

**Поиск элемента:**
1.  Вычисляем $i = H_{out}(x)$.
2.  Берем параметры внутренней функции $h_i$ для этой ячейки.
3.  Вычисляем позицию во внутренней таблице: $pos = h_i(x)$.
4.  Проверяем: если в ячейке $pos$ лежит $x$ — нашли. Если пусто или другой элемент — $x$ в таблице нет.

**Итог по совершенному хешированию:**
*   **Время построения:** $O(N)$ в среднем (randomized construction).
*   **Память:** $O(N)$ гарантированно (за счет перебора $H_{out}$).
*   **Время запроса (Find):** $O(1)$ в худшем случае (детерминировано, всего 2 вычисления хеша и 2 доступа к памяти).