# Лекция 1: Строковые Алгоритмы: Хеширование, Префикс-функция, Z-функция, Алгоритм Манакера.


## 1. Введение И Постановка Задачи
*   **Строка** ($S$) — последовательность символов $S_0, S_1, \dots, S_{n-1}$ из конечного алфавита (обычно $a-z$).
*   **Основная задача:** Даны шаблон $P$ и текст $T$. Нужно найти все вхождения $P$ в $T$.

---

## 2. Полиномиальное Хеширование (Алгоритм Рабина-Карпа)

**Идея:** Сопоставить строке число (хеш) так, чтобы сравнение строк сводилось к сравнению чисел ($O(1)$).

**Определение хеша:**
$$H(S) = (S_0 \cdot B^{n-1} + S_1 \cdot B^{n-2} + \dots + S_{n-1}) \pmod M$$
*   $M$ — большое простое число (для минимизации коллизий).
*   $B$ — случайное число, больше размера алфавита.
*   Символы кодируются числами, начиная с 1 (не с 0, чтобы различать строки вида "a" и "aa").

**Свойства:**
*   Если $H(S) \neq H(P)$, то строки точно не равны.
*   Если $H(S) = H(P)$, строки равны с высокой вероятностью (возможны коллизии). Вероятность коллизии $\approx \frac{N}{M}$.
*   Для надежности можно использовать двойное хеширование (два разных модуля $M_1, M_2$).

**Хеши подстрок за $O(1)$:**
Используется метод префиксных хешей.
1.  Считаем хеши всех префиксов: $Pref[i] = (Pref[i-1] \cdot B + S[i]) \pmod M$.
2.  Хеш подстроки $S[l \dots r]$ вычисляется по формуле:
    $$H(S[l \dots r]) = (Pref[r] - Pref[l-1] \cdot B^{r-l+1}) \pmod M$$
    *(нужен предпосчет степеней $B$)*.

**Решение задачи поиска:**
1.  Считаем $H(P)$.
2.  Проходим по тексту $T$, вычисляя хеши всех подстрок длины $|P|$.
3.  Сравниваем с $H(P)$.
**Асимптотика:** $O(|P| + |T|)$.

---

## 3. Префикс-функция (Prefix function)

**Определение:**
Массив $\pi$ длины $N$, где $\pi[i]$ — длина максимального **собственного суффикса** подстроки $S[0 \dots i]$, который совпадает с её **префиксом**.
*   *Собственный суффикс* — суффикс, не равный всей строке.
*   $\pi[0] = 0$.

**Алгоритм вычисления:**
Идем слева направо ($i$ от 1 до $N-1$). Пусть $j$ — текущая длина супрефикса (изначально $\pi[i-1]$).
1.  Пытаемся продлить супрефикс символом $S[i]$.
2.  Если $S[j] \neq S[i]$, уменьшаем длину кандидата: $j = \pi[j-1]$. Повторяем, пока не найдем совпадение или $j$ не станет 0.
3.  Если $S[j] == S[i]$, увеличиваем $j$ на 1.
4.  Записываем $\pi[i] = j$.

**Применение для поиска подстроки (КМП):**
Создаем строку $S' = P \# T$ (где $\#$ — уникальный разделитель).
Если $\pi[i] == |P|$, значит, в этой позиции заканчивается вхождение шаблона в текст.

**Асимптотика:** $O(N)$. Переменная $j$ суммарно увеличивается не более $N$ раз и уменьшается не более $N$ раз.

---

## 4. Z-функция

**Определение:**
Массив $Z$ длины $N$, где $Z[i]$ — длина наибольшего общего префикса между строкой $S$ и её суффиксом, начинающимся в позиции $i$ ($S[i \dots]$).
*   Простыми словами: сколько символов, начиная с $i$, совпадают с началом строки.
*   $Z[0]$ обычно не имеет смысла (или 0, или $N$).

**Алгоритм вычисления (эффективный):**
Идем слева направо. Поддерживаем границы **самого правого найденного Z-блока** $[L, R]$ (отрезка, совпадающего с префиксом).
Для текущего $i$:
1.  Если $i \le R$ (мы внутри блока), мы можем инициализировать $Z[i]$ уже известным значением, используя свойство симметрии:
    $$Z[i] = \min(Z[i - L], R - i + 1)$$
2.  Далее пытаемся наивно увеличить $Z[i]$, сравнивая символы $S[Z[i]]$ и $S[i + Z[i]]$.
3.  Если вышли за пределы старого $R$ ($i + Z[i] - 1 > R$), обновляем $L$ и $R$.

**Применение для поиска подстроки:**
Аналогично префикс-функции: $S' = P \# T$. Если $Z[i] == |P|$, то в позиции $i$ начинается вхождение $P$.

**Асимптотика:** $O(N)$. Правая граница $R$ только увеличивается.

---

## 5. Алгоритм Манакера (Поиск палиндромов)

**Задача:** Найти все подпалиндромы в строке (количество или самые длинные).
Палиндромы бывают двух типов:
1.  Нечетной длины (центр в символе: "aba").
2.  Четной длины (центр между символами: "abba").

**Трюк с решетками:**
Чтобы свести задачу к одному типу (нечетным палиндромам), вставляем спецсимвол (например, `#`) между всеми буквами и по краям.
*   `aba` -> `#a#b#a#`
*   `abba` -> `#a#b#b#a#`
Теперь любой палиндром имеет нечетную длину и центр в каком-то символе.

**Алгоритм:**
Ищем массив $d[i]$ — радиус палиндрома с центром в $i$.
Используется идея, аналогичная Z-функции (поддержание самого правого палиндрома $[L, R]$).
1.  Зеркально отражаем $i$ относительно центра текущего палиндрома: $j = L + R - i$.
2.  Инициализируем $d[i] = \min(d[j], R - i + 1)$.
3.  Наивно расширяем палиндром влево и вправо, пока символы совпадают.
4.  При необходимости обновляем $L$ и $R$.

**Асимптотика:** $O(N)$. Аналогично доказывается через сдвиг правой границы $R$.

---

## Итог
Все рассмотренные алгоритмы (кроме наивного хеширования без предпосчета) решают задачи поиска или анализа структуры строки за **линейное время** $O(N)$.

*   **Хеши:** Просто писать, но вероятностные ошибки.
*   **Префикс и Z-функции:** Точные детерминированные алгоритмы для поиска шаблона.
*   **Манакер:** Эффективный поиск всех палиндромов.