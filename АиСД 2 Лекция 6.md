# Лекция 6 Критерий Точки Сочленения, Взвешенные Графы, BFS, 0-K BFS, Двусторонний BFS, Алгоритм Дейкстры.

---

# Конспект Лекции №6: Кратчайшие Пути Во Взвешенных Графах

## 1. Точки Сочленения (завершение Прошлой темы)

**Определение:** Точка сочленения в неориентированном графе — это вершина, удаление которой увеличивает количество компонент связности.
*В отличие от моста (удаление которого делит компоненту на две), удаление вершины может разбить граф на $K$ компонент («лепестков»).*

**Критерий точки сочленения (в дереве обхода DFS):**
1.  **Если вершина $V$ — корень дерева обхода:**
    *   $V$ является точкой сочленения $\Leftrightarrow$ у $V$ в дереве обхода **более одного ребенка**.
    *   *Пояснение:* Если у корня два поддерева, то пройти из одного в другое можно только через корень (иначе они бы объединились в одно при обходе).
2.  **Если вершина $V$ — не корень:**
    *   $V$ является точкой сочленения $\Leftrightarrow$ у $V$ существует такой ребенок $to$, что из поддерева $to$ **нет обратного ребра** в предка $V$ (или выше $V$).
    *   *Формально:* $low[to] \ge tin[V]$.
    *   *Логика:* Если из поддерева нельзя «прыгнуть» выше $V$, то удаление $V$ изолирует это поддерево.

---

## 2. Взвешенные Графы: Основные Понятия

*   **Взвешенный граф:** $G = (V, E, W)$, где $W$ — весовая функция, сопоставляющая каждому ребру число (вес/стоимость).
*   **Вес пути:** Сумма весов ребер, входящих в путь.
*   **$dist(s, t)$:** Минимальный возможный вес пути от $S$ до $T$.

**Граничные случаи:**
1.  Пути нет: $dist = +\infty$.
2.  Есть цикл отрицательного веса: $dist = -\infty$ (минимум не существует, можно крутиться в цикле бесконечно).

> **Важное ограничение на эту лекцию:** Все веса ребер **неотрицательны** ($W \ge 0$). Случай отрицательных весов будет рассмотрен позже.

---

## 3. Поиск В Ширину (BFS)
*Применим, если все веса ребер равны 1 (или одинаковой константе).*

**Идея:** Ищем кратчайшие пути по слоям (расстояние 0, 1, 2 и т.д.).
**Процедура `Expand(v)` (Раскрытие вершины):**
*   Перебираем все исходящие ребра из $V$.
*   Если видим вершину $to$, где еще не были ($dist[to] = \infty$), обновляем ей расстояние ($dist[to] = dist[V] + 1$) и кладем в очередь.

**Алгоритм:**
1.  Очередь $Q$. Изначально в $Q$ только $S$, $dist[S]=0$, остальные $\infty$.
2.  Пока $Q$ не пуста:
    *   Достаем вершину $V$.
    *   Раскрываем её (добавляем непосещенных соседей в конец очереди).

**Сложность:** $O(V + E)$.

---

## 4. 0-K BFS
*Применим, если веса ребер — целые числа от $0$ до $K$ ($K$ — небольшое число).*

**Идея:** Вместо одной очереди используем несколько очередей (бакетов), соответствующих текущему расстоянию.
*   $Q_x$ — очередь вершин с расстоянием $x$.
*   При переходе по ребру весом $w$, новая вершина попадает в очередь $Q_{current\_dist + w}$.

**Алгоритм:**
1.  Заводим очереди $Q_0, Q_1, \dots, Q_{NK}$. (На практике достаточно массива очередей или циклический буфер размером $K+1$).
2.  Идем по очередям последовательно ($0, 1, 2 \dots$).
3.  Если очередь пуста — пропускаем. Если нет — достаем вершины и раскрываем.
4.  Важно: вершина может попасть в несколько очередей, обрабатываем её только первый раз (используем флаг `expanded`).

**Сложность:** $O(E + K \cdot V)$.
*Эффективно, если $K$ мало.*

---

## 5. Двусторонний BFS (Bidirectional BFS)
*Задача: Найти кратчайший путь между конкретными $S$ и $T$.*

**Идея:** Запускаем два BFS одновременно:
1.  Из $S$ по исходящим ребрам.
2.  Из $T$ по входящим ребрам (в обратную сторону).
Расширяем «облака» поиска послойно (слой слева, слой справа).

**Условие остановки:** Когда слои поисков пересеклись (появилась вершина, посещенная и от $S$, и от $T$).

**Ответ:**
Это **не** обязательно вершина встречи. Ответ вычисляется как:
$$ \min_{v \in \text{пересечение}} (dist_S[v] + dist_T[v]) $$

**Зачем это нужно?**
*   Если расстояние $L$, обычный BFS посетит $O(b^L)$ вершин (где $b$ — коэффициент ветвления).
*   Двусторонний BFS посетит $O(b^{L/2} + b^{L/2})$, что значительно меньше (корень из исходного количества состояний).
*   *Пример:* Решение головоломок (Кубик Рубика), где граф состояний огромен, но нужен путь между двумя конкретными точками.

**Реализация:** Для хранения посещенных вершин лучше использовать хеш-таблицы, так как граф может быть задан неявно.

---

## 6. Алгоритм Дейкстры
*Решает задачу поиска кратчайших путей от одной вершины до всех остальных во взвешенном графе с неотрицательными весами.*

**Суть (жадный подход):** На каждом шаге выбираем **нераскрытую** вершину с **минимальным** текущим расстоянием $dist[V]$ и раскрываем её (релаксируем ребра).

**Алгоритм:**
1.  $dist[v] = \infty$ для всех $v$, $dist[S] = 0$.
2.  Множество нераскрытых вершин.
3.  Пока есть нераскрытые вершины с $dist < \infty$:
    *   Выбрать вершину $V$ с минимальным $dist[V]$.
    *   Пометить $V$ как раскрытую.
    *   **Релаксация:** Для всех соседей $to$ (через ребро весом $w$):
        $$ \text{если } dist[to] > dist[V] + w \implies dist[to] = dist[V] + w $$

**Асимптотика и структуры данных:**
1.  **Наивная реализация (массив):**
    *   Поиск минимума линейным проходом: $O(V)$.
    *   Всего шагов $V$, плюс просмотр всех ребер $E$.
    *   Итого: $O(V^2 + E)$. *Выгодно для плотных графов ($E \approx V^2$).*
2.  **С кучей (Binary Heap / `std::set` / `priority_queue`):**
    *   Храним пары $(dist, v)$.
    *   Извлечение минимума: $O(\log V)$.
    *   Обновление расстояния (decrease key): $O(\log V)$.
    *   Итого: $O(E \log V)$. *Выгодно для разреженных графов.*
3.  **Куча Фибоначчи (теоретическая оптимизация):**
    *   $O(E + V \log V)$.

**Доказательство корректности (идея):**
Индукция/От противного.
Утверждается, что в момент, когда мы достаем вершину $V$ из приоритетной очереди (выбираем как минимум), $dist[V]$ уже является истинным кратчайшим расстоянием.
*Доказательство:* Пусть $V$ — первая вершина, для которой это не так. Значит, существует более короткий путь. Этот путь должен где-то покинуть множество уже раскрытых вершин через некоторую вершину $X$. Но так как веса неотрицательны, путь до $X$ уже короче или равен пути до $V$. А так как мы выбрали $V$ как минимум, то $dist[V] \le dist[X]$. Противоречие.