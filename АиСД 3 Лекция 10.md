# Лекция 10: Базовая Вычислительная Геометрия

## 1. Введение И Работа С Точностью
Геометрия на компьютере сопряжена с проблемой точности представления вещественных чисел.
*   **Проблема:** Вещественные числа (`double`) хранятся с погрешностью. Прямое сравнение (`==`) может давать неверные результаты.
*   **Решение:** Все сравнения делаем с погрешностью $\varepsilon$ (эпсилон). Обычно $\varepsilon \approx 10^{-9}$.
    *   Равенство: $a = b \iff |a - b| \le \varepsilon$.
    *   Совет: Если возможно, решать задачу в целых числах (например, хранить числитель и знаменатель дроби отдельно), переходя к `double` только в самом конце.

## 2. Точки И Векторы
Для упрощения кода понятия «точка» и «радиус-вектор» смешиваются в одну структуру `Point {x, y}`.

**Основные операции:**
1.  **Сложение:** Покомпонентно. Геометрически — сложение векторов (правило параллелограмма) или нахождение середины отрезка (сумма делить на 2).
2.  **Вычитание:** $B - A$ дает вектор $\vec{AB}$.
3.  **Умножение на число:** Масштабирование вектора.

**Произведения векторов:**
*   **Скалярное произведение (Dot Product):**
    *   Формула: $x_1x_2 + y_1y_2 = |a| \cdot |b| \cdot \cos \phi$.
    *   Применение: Определение типа угла (не зная самого угла):
        *   $> 0$ — угол острый.
        *   $= 0$ — векторы перпендикулярны.
        *   $< 0$ — угол тупой.
*   **Векторное (косое) произведение (Cross Product):**
    *   Формула (для 2D): $x_1y_2 - x_2y_1 = |a| \cdot |b| \cdot \sin \phi$ (ориентированный угол).
    *   Геометрический смысл: Ориентированная площадь параллелограмма, натянутого на векторы.
    *   Применение: Определение взаимной ориентации:
        *   $> 0$ — поворот от A к B против часовой стрелки (левая тройка).
        *   $< 0$ — поворот по часовой стрелке.
        *   $= 0$ — векторы коллинеарны (сонаправлены или противоположны — проверяется через скалярное произведение).

*   **Нахождение угла:** Лучший способ найти точный угол — функция `atan2(cross, dot)`, так как она учитывает знаки синуса и косинуса.

## 3. Площадь Многоугольника
Многоугольник задается массивом вершин в порядке обхода.

**Метод треугольников:**
*   Идея: Суммируем ориентированные площади треугольников, образованных началом координат $(0,0)$ и каждой стороной многоугольника.
*   Формула: $S = \frac{1}{2} \left| \sum_{i=0}^{n-1} (P_i \times P_{i+1}) \right|$.
*   Все площади «снаружи» сократятся за счет знаков векторного произведения.
*   Знак суммы без модуля говорит об ориентации обхода (против часовой — плюс, по часовой — минус).

**Метод трапеций:**
*   Идея: Проецируем стороны на ось X, считаем площади трапеций под ними.
*   Удобен для «клетчатых» многоугольников (стороны параллельны осям), так как трапеции вырождаются в прямоугольники.

## 4. Прямые
**Задание прямой:** Общее уравнение $Ax + By + C = 0$.
*   Позволяет задать вертикальные прямые (в отличие от $y = kx + b$).
*   Если строить по двум точкам $(x_1, y_1)$ и $(x_2, y_2)$:
    *   $A = y_1 - y_2$
    *   $B = x_2 - x_1$
    *   $C = -Ax_1 - By_1$
    *   При целых координатах точек коэффициенты $A, B, C$ остаются целыми.

**Свойства:**
*   Вектор нормали: $\vec{n} = (A, B)$.
*   Направляющий вектор: $\vec{v} = (-B, A)$.

**Расстояние от точки до прямой:**
1.  Нормализуем уравнение прямой: делим $A, B, C$ на $\sqrt{A^2 + B^2}$, чтобы длина вектора нормали стала равна 1.
2.  Подставляем точку $(x_0, y_0)$ в уравнение: $dist = |A_{norm}x_0 + B_{norm}y_0 + C_{norm}|$.

**Пересечение двух прямых:**
*   Решаем систему уравнений методом Крамера (чтобы избежать лишних `if` и делений на ноль раньше времени).
*   Считаем определитель матрицы коэффициентов $\Delta$.
    *   Если $\Delta \neq 0$: пересечение в одной точке.
    *   Если $\Delta = 0$: прямые параллельны или совпадают (проверяется через миноры).

## 5. Окружности
Задаются центром $(x_c, y_c)$ и радиусом $R$.

**Пересечение прямой и окружности:**
1.  Найти расстояние $d$ от центра окружности до прямой.
2.  Сравнить $d$ и $R$:
    *   $d > R$: нет пересечений.
    *   $d \approx R$: 1 точка (касание). Точка — проекция центра на прямую.
    *   $d < R$: 2 точки. Находим проекцию центра $H$, отступаем от неё в обе стороны вдоль прямой на расстояние $\sqrt{R^2 - d^2}$.

**Пересечение двух окружностей:**
1.  Вычитаем уравнение одной окружности из другой ($Eq_1 - Eq_2$).
2.  Квадраты ($x^2, y^2$) сокращаются, остается линейное уравнение $Ax + By + C = 0$.
3.  Это уравнение прямой (радикальной оси), проходящей через точки пересечения.
4.  Задача сводится к пересечению этой прямой с любой из окружностей.

## 6. Триангуляция Многоугольника
Задача: разбить произвольный (возможно невыпуклый) многоугольник на треугольники диагоналями, не пересекающимися внутри.

**Понятие «Уха»:**
Три последовательные вершины $P_{i-1}, P_i, P_{i+1}$ образуют «ухо», если диагональ $P_{i-1}P_{i+1}$ лежит строго внутри многоугольника (в треугольнике нет других вершин).

**Теорема:** В любом $n$-угольнике ($n \ge 4$) есть хотя бы два непересекающихся уха.

**Алгоритм триангуляции (метод отрезания ушей):**
1.  Находим самую левую нижнюю вершину. Угол при ней гарантированно выпуклый (< 180°).
2.  Рассматриваем треугольник, образованный этой вершиной и её соседями.
3.  Проверяем, попали ли внутрь этого треугольника другие вершины многоугольника.
    *   **Случай А (пусто):** Это «ухо». Отрезаем его (проводим диагональ между соседями). Решаем задачу для $n-1$ вершин.
    *   **Случай Б (есть вершины):** Находим вершину внутри треугольника, наиболее удаленную от отсекающей прямой. Соединяем исходную вершину с ней диагональю. Многоугольник распадается на два меньших. Рекурсивно решаем для обоих.

**Асимптотика:** Алгоритм работает за $O(N^2)$.