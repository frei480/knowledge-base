---
tags: machinelearning
Автор: Andrej Karpathy
Ссылка: https://www.youtube.com/watch?v=PaCmpygFfXo
Тип: Пересказ видео
Название: "The spelled-out intro to neural networks and backpropagation: building micrograd"
---
Подробное введение в языковое моделирование: создание makemore

00:00 Введение в Make More

• Make More — это репозиторий на GitHub, который будет создаваться шаг за шагом.
• Пример использования: генерация уникальных имён на основе набора данных «имена.txt».
• Make More помогает создавать имена, которые звучат как настоящие, но на самом деле являются уникальными.

00:47 Принцип работы Make More

• Make More — это языковая модель на уровне персонажей, которая обрабатывает строки как последовательности символов.
• Модель предсказывает следующий символ в последовательности на основе предыдущих символов.
• Примеры генерации имён: «не говори никому», «ирот», «зени».

01:45 Типы языковых моделей

• Планируется реализация различных языковых моделей: биграммы, многоуровневые персептроны, рекуррентные нейронные сети, трансформаторы.
• Трансформатор, который будет построен, эквивалентен GPT-2.

02:36 Начало работы с данными

• Загрузка набора данных «имена.txt» в формате txt.
• Разделение строки на слова с помощью функции разделения строк в Python.
• Анализ характеристик набора данных: количество слов, длина самого короткого и самого длинного слова.

04:27 Статистическая структура слов

• Каждое слово содержит много статистической информации о последовательности символов.
• Пример: слово «Изабелла» предоставляет информацию о вероятности появления определённых символов в последовательности.

05:23 Модель биграмм

• Модель биграмм работает с двумя символами одновременно, предсказывая следующий символ на основе предыдущего.
• Пример работы с биграммами: анализ слов «Эмма», «Оливия», «Ева».

09:14 Подсчёт биграмм

• Подсчёт частоты появления биграмм в тренировочном наборе данных.
• Использование словаря для учёта биграмм.
• Пример подсчёта: буква «а» встречается три раза, так как все слова заканчиваются этой буквой.

10:46 Повторение для всех слов

• Повторение процесса подсчёта биграмм для всех слов из набора данных.

10:51 Анализ данных в Python

• Использование функции `b.items()` для получения кортежей, где ключи — символы в граммах, а значения — количество.
• Сортировка кортежей по значениям с помощью лямбда-выражения `key=lambda x: x[1]`.
• Пример сортировки: биграммы `q` и `r` встречаются только один раз, `dz` — тоже один раз.

12:38 Переход к двумерному массиву

• Переход от словаря Python к двумерному массиву для более удобного хранения данных.
• Представление массива в библиотеке PyTorch, где строки — первый символ биграммы, а столбцы — второй символ.
• Импорт PyTorch и создание массива нулей размером 3x5.

13:37 Работа с тензорами в PyTorch

• Настройка типа данных массива на `torch.int32` для представления целых чисел.
• Индексация массива для изменения отдельных записей.
• Пример индексации: установка значения в позиции `(1, 3)` равным 1.

15:26 Создание таблицы поиска

• Объединение всех слов в одну строку и преобразование в набор символов.
• Сортировка набора символов от `a` до `z` и создание таблицы поиска `s2i`.
• Сопоставление символов с целыми числами для индексации массива.

17:42 Визуализация массива

• Использование библиотеки Matplotlib для визуализации массива.
• Инверсия таблицы поиска для отображения символов в целые числа.
• Вывод биграмм и их частот на график.

20:49 Оптимизация структуры массива

• Обнаружение пустых строк и столбцов в массиве из-за специальных символов `s` и `e`.
• Решение проблемы путём добавления одного специального символа и смещения остальных букв.
• Пересмотр структуры массива для более эффективного использования пространства.

23:30 Анализ массива подсчётов

• Массив подсчётов содержит информацию о первых буквах слов.
• Структура массива позволяет выполнять выборку из языковой модели на уровне символов и биграмм.
• Начало выборки начинается с символической точки.

24:30 Индексация и преобразование данных

• Индексация массива позволяет получить первую строку данных.
• Форма первой строки — 27 чисел.
• Преобразование целых чисел в числа с плавающей запятой для нормализации.

25:29 Нормализация и создание вектора вероятностей

• Нормализация данных путём деления на сумму для создания распределения вероятностей.
• Сумма вероятностей должна быть равна единице.

26:09 Выборка из распределения вероятностей

• Использование мультиномиального распределения torch.multinomial для выборки.
• Применение объекта-генератора для детерминированности результатов.

27:12 Пример использования генератора

• Создание объекта-генератора и использование его для генерации случайных чисел.
• Нормализация случайных чисел для получения распределения вероятностей.

28:13 Анализ выборок

• Запрос количества выборок и настройка параметра «замена» для повторной выборки элементов.
• Анализ распределения вероятностей в выборках.

30:09 Генерация первого символа

• Выбор первого символа на основе выборки из распределения.
• Пример выбора буквы «м» как первого символа.

31:15 Генерация последующих символов

• Поиск следующей строки данных для выбранного символа.
• Нормализация значений и генерация следующего символа.

33:02 Завершение генерации

• Проверка конечного токена и завершение генерации.
• Добавление сгенерированных символов в список.

33:44 Проблемы с генерацией имён

• Языковая модель Байрама генерирует бессмысленные имена.
• Примеры имён не похожи друг на друга.
• Модель не учитывает контекст предыдущих символов.

35:26 Попытка улучшения генерации

• Использование равномерного распределения вероятностей для генерации имён.
• Необученная модель генерирует случайные имена.
• Обученная модель генерирует более осмысленные имена.

36:26 Оптимизация процесса

• Извлечение строки из матрицы подсчётов неэффективно.
• Подготовка матрицы вероятностей для повышения эффективности.
• Практическое использование n-мерных тензоров и радиовещания.

37:42 Суммирование и деление строк

• Суммирование строк матрицы для получения вектора сумм.
• Использование torch.sum для суммирования по строкам.
• Важность понимания семантики вещания для операций с тензорами.

41:44 Применение правил вещания

• Проверка возможности деления массива на вектор.
• Применение правил вещания для выполнения операции деления.
• Копирование вектора для поэлементного деления.

43:19 Проверка результата

• Проверка нормализации строк после деления.
• Подтверждение правильности нормализации.
• Рекомендация ознакомиться с семантикой вещания для дальнейшего использования.

44:47 Анализ формы тензора

• Рассматривается тензор формы 27 на 1.
• Обсуждается влияние ключевого слова «сохранить их» на форму тензора.
• Упоминается, что при использовании «сохранить их» как false первое измерение сжимается.

45:46 Правила трансляции

• Объясняются правила трансляции: выравнивание размеров справа и проверка равенства или существования измерений.
• Подчёркивается, что в данном случае все измерения равны, поэтому трансляция должна работать.

46:43 Ошибка при трансляции

• При попытке трансляции получается неверный результат.
• Если оставить «сохранить их» как true, трансляция работает корректно.

47:22 Объяснение проблемы

• Объясняется, что при делении 27 на 27 на 1 на 27 Torch копирует вектор строк 27 раз по вертикали.
• Нормализация происходит по столбцам, а не по строкам, что приводит к неверным результатам.

48:23 Итог проблемы

• Проблема возникает из-за молчаливого добавления измерения при трансляции.
• Подчёркивается важность понимания правил трансляции для избежания ошибок.

50:27 Оценка качества модели

• Обсуждается оценка качества модели через потери при обучении.
• Рассматривается вероятность, которую модель присваивает каждой биграмме.

54:00 Логарифмическая вероятность

• Объясняется использование логарифмической вероятности для удобства работы с вероятностями.
• Логарифмическая вероятность начинается с нуля и накапливается, отражая качество модели.

55:39 Математическая связь

• Доказывается, что логарифмическая вероятность равна сумме логарифмов отдельных вероятностей.
• Это позволяет легко суммировать вероятности для оценки качества модели.

56:05 Логарифмическая вероятность и отрицательное логарифмическое правдоподобие

• Логарифмическая вероятность стремится к нулю при всех вероятностях, равных единице.
• Отрицательное логарифмическое правдоподобие — это отрицательная величина логарифмической вероятности.
• Функция потерь должна быть минимизирована, поэтому отрицательное логарифмическое правдоподобие инвертируется.

57:03 Нормализация логарифмической вероятности

• Для удобства используется нормализация логарифмической вероятности, обычно среднее значение.
• Средняя логарифмическая вероятность становится функцией потерь.
• Задача обучения — найти параметры, минимизирующие потерю логарифмической вероятности.

58:50 Максимизация вероятности и оптимизация

• Максимизация вероятности эквивалентна максимизации логарифмической вероятности из-за монотонности логарифмической функции.
• Оптимизация логарифмической вероятности эквивалентна минимизации отрицательной логарифмической вероятности.

59:46 Оценка вероятности слов

• Можно оценить вероятность любого слова, например, «андре».
• Если вероятность слова равна нулю, логарифм этой вероятности равен отрицательной бесконечности.
• Это приводит к бесконечным потерям, что нежелательно.

01:01:52 Сглаживание модели

• Сглаживание модели добавляет поддельные аккаунты, чтобы избежать нулей в вероятностной матрице.
• Добавление единицы к вероятностям делает модель более однородной.
• Сглаживание предотвращает бесконечные потери и улучшает качество модели.

01:03:40 Альтернативный подход с нейронной сетью

• Проблема моделирования языка на уровне символов байрама переносится в структуру нейронной сети.
• Нейронная сеть получает один символ в качестве входных данных и выводит распределение вероятностей по следующему символу.
• Функция потерь отрицательная логарифмическая вероятность используется для оценки настройки параметров нейронной сети.

01:05:36 Создание обучающего набора

• Обучающий набор состоит из двух списков: исходных данных и целей.
• Исходные данные — это биграммы, а цели — метки следующего символа.
• Биграммы обозначаются как x и y, где x — первый символ, а y — второй.
• Тензоры создаются из списков целых чисел для удобства работы с данными.

01:07:05 Примеры для нейронной сети

• В наборе данных есть пять примеров ввода для нейронной сети.
• При входных данных 0 желаемая метка — 5.
• При входных данных 5 мы хотим, чтобы вероятность 13 была высокой.
• При входных данных 13 мы также хотим, чтобы вероятность 13 была высокой.
• При входе 1 мы хотим, чтобы вероятность 0 была высокой.

01:07:54 Предупреждение о API-интерфейсах

• Будьте осторожны с API-интерфейсами фреймворков, например, с torch.tensor и torch.Tensor.
• Разница между ними не всегда очевидна из документации.
• Рекомендуется использовать torch.tensor для работы с целыми числами.

01:10:06 Однократное кодирование

• Целые числа нельзя напрямую вводить в нейронную сеть.
• Однократное кодирование преобразует целые числа в векторы с единицами в соответствующих позициях.
• В PyTorch есть функция для однократного кодирования.

01:11:14 Реализация однократного кодирования

• Импортируем torch и используем функцию one_hot.
• Вводим целые числа и указываем количество классов.
• Получаем закодированные векторы для пяти примеров.

01:12:59 Типы данных в кодировке

• Для ввода в нейронные сети нужны числа с плавающей запятой, а не целые числа.
• По умолчанию dtype в кодировке — 64-разрядное целое число.
• Устанавливаем dtype на float32 для совместимости с нейронными ядрами.

01:14:00 Инициализация весов нейрона

• Инициализируем веса нейрона случайными числами из нормального распределения.
• Размер весов — 27 на 1.
• Умножаем кодировку x на веса w для вычисления активаций нейрона.

01:16:15 Параллельная оценка нейронов

• Вместо одного нейрона используем 27 нейронов для параллельной оценки.
• Это позволяет лучше использовать информацию из входных данных.
• Результат умножения 5 на 27 и 27 на 27 даёт форму 5 на 27, где каждый элемент представляет активацию одного из 27 нейронов.

01:17:07 Матричное умножение в нейронных сетях

• Матричное умножение позволяет эффективно оценивать скалярное произведение между множеством входных примеров и нейронов.
• Пример: скалярное произведение между 3-м входным сигналом и 13-м столбцом матрицы W.
• Параллельное выполнение операций умножения матриц для всех входных примеров и выходных нейронов.

01:18:17 Структура нейронной сети

• Входной слой нейронной сети содержит 27 нейронов, каждый из которых выполняет операцию умножения на W и X.
• Отсутствие смещения и нелинейности в нейронах, сеть состоит только из одного линейного слоя.

01:19:16 Интерпретация выходных данных

• Выходные данные представляют собой распределение вероятности для следующего символа в последовательности.
• Необходимость нормализации значений для получения вероятностей.

01:20:15 Логарифмические подсчёты

• Выходные данные интерпретируются как логарифмические подсчёты, а не как прямые подсчёты.
• Возведение в степень отрицательных чисел даёт значения меньше единицы, а положительных — больше единицы.

01:23:32 Нормализация вероятностей

• Нормализация строк матрицы подсчётов для получения вероятностей.
• Выходные данные соответствуют вероятностям благодаря проведённым преобразованиям.

01:25:00 Дифференцируемые операции

• Использование дифференцируемых операций для вычисления распределений вероятностей.
• Пример ввода точки в нейронную сеть через однократное кодирование.

01:25:59 Оптимизация и функция потерь

• Оптимизация весовых коэффициентов W для получения хороших вероятностей.
• Функция потерь как способ измерения качества результатов.

01:26:48 Слой «софтмакс»

• Слой «софтмакс» нормализует логиты, возводит их в степень и делит, получая распределение вероятностей.
• «Софтмакс» позволяет нейронной сети вычислять вероятности, суммирующиеся с единицей.

01:28:13 Прямой проход и дифференцируемые слои

• Прямой проход вычисляет вероятность выхода нейронной сети.
• Все слои являются дифференцируемыми, что позволяет выполнять обратное распространение.
• Обратное распространение включает умножение, сложение, возведение в степень и деление.

01:29:11 Пример биграммы

• Рассматривается пример биграммы с буквой «е» после точки.
• Входные данные — индекс 0, выходные данные — вероятности 27 чисел.
• Метка 5 указывает на правильный символ.

01:30:08 Анализ вероятностей и потерь

• Сеть считает вероятность символа «е» после точки очень низкой — 1%.
• Логарифмическая вероятность отрицательного значения высока, что приводит к большим потерям.
• Потери определяются как среднее значение вероятности отрицательного логарифма.

01:31:01 Оптимизация параметров

• Изменение значения «w» улучшает результаты.
• Повторная выборка с новым значением «w» снижает потери до 3,37.
• Оптимизация начинается со случайной догадки и продолжается до минимизации потерь.

01:32:29 Принцип работы микрограва

• Микрограв использует дифференцируемые операции для минимизации потерь.
• Градиенты потерь относительно параметров «w» вычисляются и используются для обновления параметров.

01:35:31 Оценка потерь в классификации

• Для классификации используется отрицательная логарифмическая вероятность.
• Вероятности с правильными индексами используются для расчёта потерь.
• Векторизованная форма выражения позволяет эффективно рассчитывать потери.

01:38:48 Подготовка к обратному распространению

• Градиенты сбрасываются до нуля.
• В PyTorch можно установить градиенты равными «нет», что эквивалентно нулю.
• Параметр requires_grad is true необходим для вычисления градиента.

01:39:40 Работа с вычислительным графиком в PyTorch

• PyTorch строит полный вычислительный график, отслеживая все операции под капотом.
• График содержит зависимости и математические операции.
• После подсчёта потерь можно заполнить градиенты промежуточных значений.

01:40:29 Анализ градиентов

• Градиенты показывают влияние каждого веса на функцию потерь.
• Положительные градиенты указывают на увеличение потерь при изменении веса.
• Отрицательные градиенты указывают на уменьшение потерь при изменении веса.

01:41:28 Обновление параметров

• Обновление параметров происходит через умножение на градиент.
• После обновления потерь должны уменьшиться.
• Процесс повторяется до достижения низких потерь.

01:42:48 Оптимизация биграммного набора данных

• Создание набора данных биграмм с 228 примерами.
• Увеличение скорости обучения для улучшения результатов.
• Оптимизация всего обучающего набора биграмм.

01:45:29 Сравнение подходов

• Явный подход хорошо оптимизирует функцию потерь без градиентной оптимизации.
• Градиентный подход более гибкий и позволяет усложнять нейронные сети.
• Логиты на выходе нейронной сети подвергаются преобразованию softmax.

01:47:27 Масштабируемость подхода

• Явный подход не масштабируется для большого количества символов.
• Подход на основе нейронных сетей более масштабируем.
• Матричное умножение влияет на взаимодействие нейронов.

01:48:08 Взаимодействие нейронов через матричное умножение

• Умножение горячего вектора на матрицу w приводит к удалению определённых строк w.
• Это объясняет, как отдельные нейроны взаимодействуют через матричное умножение.

01:48:36 Индексация биграммы

• Первый символ биграммы индексируется в массив для получения распределения вероятностей следующего символа.
• Индекс кодируется как единица hot и умножается на w, что даёт logit.
• Logit возводится в степень для создания подсчётов, затем нормализуется и становится вероятностью.

01:49:35 Логарифм и инициализация

• Учитывается логарифм, а не количество отсчётов.
• Массив заполняется путём подсчёта биграмм.
• В градиентном фреймворке массив инициализируется случайным образом и оптимизируется через потери.

01:50:30 Сглаживание и регуляризация

• Сглаживание добавляет ложные значения для равномерного распределения вероятностей.
• Увеличение количества отсчётов делает распределение более однородным.
• Регуляризация стимулирует w быть близким к нулю, что эквивалентно сглаживанию.

01:51:22 Формула регуляризации

• Элементы w возводятся в квадрат и суммируются для создания потерь регуляризации.
• Потери равны нулю, если w равно нулю, иначе они накапливаются.
• Оптимизация стремится свести все вероятности к нулю и все ws к нулю.

01:54:18 Выборка из модели

• Код выборки из модели нейронной сети аналогичен предыдущему примеру.
• Текущий индекс ix кодируется в строку xc, умножается на w, и логиты нормализуются.
• Выборка из распределения даёт те же результаты, что и ранее.

01:56:41 Итоги и перспективы

• Модель языка на уровне символов биграмм обучена двумя способами, которые дают одинаковый результат.
• Первый способ: подсчёт частоты биграмм и нормализация.
• Второй способ: использование потерь с отрицательным логарифмическим правдоподобием для оптимизации матрицы подсчётов.
• Градиентный фреймворк более гибок и позволяет усложнять нейронные сети, включая трансформаторы.

