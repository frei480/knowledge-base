# Лекция 10: Деревья Поиска (BST, AVL, Splay)

## 1. Введение И Мотивация
Основная цель структур данных, рассматриваемых далее, — поддерживать множество элементов $S$ и выполнять операции:
1.  **Find(x):** Проверить наличие элемента $x$ в множестве (вернуть true/false или указатель).
2.  **Insert(x):** Добавить элемент $x$ (дубликаты обычно игнорируются).
3.  **Erase(x):** Удалить элемент $x$ (если его нет, ничего не делать).

**Опциональные операции:**
*   Вывести все элементы в отсортированном порядке (tree traversal).
*   **Merge:** Объединить два дерева.
*   **Split:** Разбить дерево на два по ключу $K$ (меньше $K$ и больше $K$).

*Отличие от хеш-таблиц:* Хеш-таблицы выполняют поиск/вставку за $O(1)$, но не поддерживают порядок элементов. Деревья поиска позволяют итерироваться по элементам в порядке возрастания.

---

## 2. Бинарное Дерево Поиска (Naive BST)

### Определение
Бинарное дерево, в вершинах которого хранятся элементы (ключи).
**Инвариант дерева поиска:** Для любой вершины $V$:
*   Все ключи в **левом** поддереве **меньше** ключа в $V$.
*   Все ключи в **правом** поддереве **больше** ключа в $V$.

### Реализация Операций (Наивная)

1.  **Find(x):**
    *   Начинаем с корня. Сравниваем $x$ с текущей вершиной.
    *   Если $x <$ текущей — идем влево.
    *   Если $x >$ текущей — идем вправо.
    *   Если равны — нашли. Если уперлись в пустоту — элемента нет.
    *   *Сложность:* $O(h)$, где $h$ — высота дерева.

2.  **Insert(x):**
    *   Запускаем процедуру поиска.
    *   Если нашли элемент — ничего не делаем.
    *   Если попытались пойти по несуществующему ребру — подвешиваем туда новый узел с $x$.

3.  **Erase(x):**
    *   Находим узел с $x$.
    *   **Случай 1:** У узла 0 или 1 ребенок.
        *   Просто удаляем узел и "подтягиваем" единственного сына (или null) на его место к родителю.
    *   **Случай 2:** У узла 2 сына.
        *   Нельзя просто удалить.
        *   Идем в **правое** поддерево и ищем там **минимум** (идем влево до упора). Назовем этот элемент $M$.
        *   У $M$ гарантированно нет левого сына.
        *   Копируем значение $M$ на место удаляемого $x$.
        *   Рекурсивно удаляем старый узел $M$ (это сводится к Случаю 1, так как у $M$ нет левого сына).

### Проблема Наивного Подхода
В худшем случае (например, при вставке отсортированной последовательности 1, 2, 3, 4, 5) дерево вырождается в "бамбук" (список).
*   Высота $h becomes N$.
*   Время работы операций $O(N)$.
*   **Цель:** Сделать высоту логарифмической $h \approx \log N$.

---

## 3. АВЛ-дерево (AVL Tree)

### Определение
Это бинарное дерево поиска, в котором для **каждой** вершины выполняется свойство сбалансированности:
$$|h(Left) - h(Right)| \le 1$$
Где $h$ — высота поддерева.
Обозначим баланс-фактор $\Delta = h(L) - h(R)$. Допустимые значения $\Delta \in \{-1, 0, 1\}$.

### Доказательство Логарифмической Высоты
Пусть $S(h)$ — минимальное число вершин в АВЛ-дереве высоты $h$.
*   $S(1) = 1$
*   $S(2) = 2$
*   Для высоты $h$ одно поддерево должно иметь высоту $h-1$, а другое минимум $h-2$.
*   Рекуррента: $S(h) = 1 + S(h-1) + S(h-2)$.

Это напоминает числа Фибоначчи ($F_n$).
Доказывается, что $S(h) = F_{h+2} - 1$.
Так как числа Фибоначчи растут экспоненциально ($F_h \approx \phi^h$), то число вершин $N$ растет экспоненциально от высоты. Следовательно, высота $h$ логарифмически зависит от $N$:
$$h \approx 1.44 \log_2 N$$

### Балансировка (Повороты)
Операции вставки/удаления могут нарушить инвариант (сделать $\Delta = \pm 2$). Исправление делается с помощью **поворотов** (rotations).

**Малый поворот (на примере правого поворота):**
*   Есть ребро между $B$ (родитель) и $A$ (левый сын).
*   "Проворачиваем" ребро: $A$ становится родителем $B$.
*   Правое поддерево $A$ становится левым поддеревом $B$.
*   Инвариант BST сохраняется.

**Алгоритм балансировки:**
После вставки/удаления мы идем от измененной вершины вверх к корню и чиним $\Delta$.
Пусть в вершине $A$ возник дисбаланс $\Delta(A) = -2$ (перевес вправо).
Пусть $B$ — правый сын $A$.

1.  **Случай 1 (Однородный):** $\Delta(B) = 0$ или $-1$.
    *   Выполняется **один малый левый поворот** вокруг ребра $A-B$.
    *   Баланс восстанавливается.
2.  **Случай 2 (Зигзаг):** $\Delta(B) = +1$ (у правого сына перевес влево).
    *   Сначала делаем **правый поворот** для правого сына $B$ (он становится правым-правым).
    *   Затем делаем **левый поворот** для самого узла $A$.
    *   (Это называется большой поворот или double rotation).

Симметрично рассматривается случай $\Delta(A) = +2$.

**Итог по AVL:**
*   Все операции гарантированно работают за $O(\log N)$.
*   Нужно хранить высоту или баланс-фактор в узлах.
*   Операции Split/Merge реализуются сложно.

---

## 4. Splay-дерево (Splay Tree)

### Идея
Самобалансирующееся дерево без жестких инвариантов высоты.
**Свойство:** При доступе к элементу $x$ (Find, Insert, даже неудачный Find), этот элемент (или его сосед, если $x$ не найден) **поднимается в корень** с помощью серии поворотов.

**Зачем:**
1.  Учет локальности запросов (часто используемые элементы будут у корня).
2.  Амортизированная сложность $O(\log N)$.
3.  Легко реализовать Split и Merge (через подъем в корень).

### Операция Splay(x)
Процесс подъема вершины $x$ в корень. Состоит из шагов, зависящих от расположения $x$, родителя ($P$) и дедушки ($G$).

1.  **Zig (Зиг):**
    *   $x$ — сын корня (дедушки нет).
    *   Выполняется 1 поворот. Используется как завершающий шаг.
2.  **Zig-Zig (Зиг-Зиг):**
    *   $x$ и $P$ находятся с одной стороны (оба левые сыновья или оба правые).
    *   **Важно:** Сначала поворачиваем **дедушку** ($P$ встает на место $G$), потом поворачиваем **родителя** ($x$ встает на место $P$).
3.  **Zig-Zag (Зиг-Заг):**
    *   $x$ и $P$ с разных сторон (один левый, другой правый).
    *   Два поворота: сначала $x$ поднимается к $P$, потом $x$ поднимается к $G$ (как в AVL двойной поворот).

### Амортизационный Анализ (Метод потенциалов)
Используем метод потенциалов для доказательства сложности.
*   $S(x)$ — size, количество вершин в поддереве $x$.
*   $R(x) = \log_2 S(x)$ — ранг вершины (примерная высота).
*   Потенциал дерева $\Phi = \sum_{v \in Tree} R(v)$.

**Учетная стоимость (Amortized Cost):**
$$A_i = T_{real} + \Phi_{after} - \Phi_{before}$$
Сумма учетных стоимостей ограничивает реальное время сверху.

**Теорема:** Учетная стоимость операции Splay не превосходит $O(\log N)$.
На лекции разобран анализ только операции **Zig**:
$$A_{zig} \le 1 + 3(R_{new}(x) - R_{old}(x))$$
Полный анализ показывает, что при схлопывании телескопической суммы, стоимость серии операций логарифмическая.

### Преимущества Splay
*   Не нужно хранить доп. информацию (высоту, цвет).
*   Работает быстрее на запросах с неравномерным распределением (локальность).
*   Удобные Split/Merge:
    *   *Merge(T1, T2):* Splay(max element in T1), он станет корнем T1 без правого сына. Подвесить T2 справа.

---

**Заключение:** AVL дает строгую гарантию $O(\log N)$ на каждый запрос. Splay дает амортизированную гарантию $O(\log N)$ (в среднем на серию запросов) и адаптируется под паттерны доступа.