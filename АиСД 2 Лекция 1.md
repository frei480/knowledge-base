---
tags:
---
# Лекция 1
---
## 1. Динамическое Программирование: Общая Идея

**Динамическое программирование (ДП)** применяется, когда:

* задачу можно разбить на подзадачи,
* решение задачи определяется решениями меньших подзадач,
* важна оптимальность (максимум / минимум).

Основная идея:

> Мы «замораживаем» состояние системы в некоторый момент времени и храним только ту информацию, которая важна для продолжения решения.

Обычно:

* заводится массив `DP`,
* `DP[i]` — оптимальный ответ для подзадачи, связанной с состоянием `i`,
* переходы строятся из уже посчитанных состояний.

Корректность доказывается **индукцией**:
если все предыдущие `DP` посчитаны правильно, то по формуле перехода правильно считается следующее.

---

## 2. Пример: Одномерное ДП (кузнечик)

Есть последовательность чисел. Кузнечик прыгает вправо и собирает сумму.

**Состояние**:

* `DP[i]` — максимальная сумма, с которой можно попасть в позицию `i`.

**Почему храним только максимум?**
Если в одну клетку можно прийти несколькими способами, нас интересует только лучший — все остальные не дадут оптимального продолжения.

**Переход**:

```
DP[i] = max(DP[i-1], DP[i-2]) + a[i]
```

**База**:

```
DP[0] = 0
```

---

## 3. ДП «назад» И ДП «вперёд»

### Обычное ДП (назад)

* идём слева направо,
* чтобы посчитать `DP[i]`, все предыдущие значения уже известны.

### ДП Вперёд

* из текущего состояния «релаксируем» будущие,
* часто удобно в задачах на графах или шахматных досках.

Пример:

* все `DP` изначально `−∞`,
* стартовое состояние = 0,
* из каждого состояния обновляем все достижимые дальше.

---

## 4. Двумерное Динамическое Программирование

### Задача «черепашка»

Дана таблица чисел. Нужно пройти:

* из левого верхнего угла в правый нижний,
* можно идти только **вправо** или **вниз**,
* максимизировать сумму.

**Состояние**:

```
DP[i][j] — максимальная сумма, 
которую можно набрать, дойдя до клетки (i, j)
```

**Переход**:

```
DP[i][j] = max(DP[i-1][j], DP[i][j-1]) + a[i][j]
```

**База**:

* первая строка и первый столбец считаются отдельно.

Жадные алгоритмы не работают — нужно учитывать весь путь.

---

## 5. Восстановление Пути

Если нужно восстановить путь:

* дополнительно храним, откуда пришли в каждую клетку,
* путь восстанавливается **с конца в начало**.

---

## 6. Задача НОП (наибольшая Общая подпоследовательность)

Даны две строки `S` и `T`. Нужно найти их **наибольшую общую подпоследовательность**.

**Состояние**:

```
DP[i][j] — длина НОП
для префиксов S[1..i] и T[1..j]
```

**База**:

```
DP[0][j] = DP[i][0] = 0
```

**Переход**:

* если `S[i] == T[j]`:

```
DP[i][j] = DP[i-1][j-1] + 1
```

* иначе:

```
DP[i][j] = max(DP[i-1][j], DP[i][j-1])
```

**Ответ**: `DP[n][m]`
Сложность: `O(nm)`.

---

## 7. Задача НВП (наибольшая Возрастающая подпоследовательность)

### Решение 1 — Классическое `O(n²)`

**Состояние**:

```
DP[i] — длина НВП, заканчивающейся в позиции i
```

**Переход**:

```
DP[i] = 1 + max(DP[j]), где j < i и a[j] < a[i]
```

---

### Решение 2 — Оптимальное (`O(n log n)`)

Идея:

* фиксируем длину подпоследовательности,
* храним **минимально возможный последний элемент**.

**DP[k]** — минимальный последний элемент
возрастающей подпоследовательности длины `k`.

Свойства:

* массив `DP` строго возрастает,
* можно использовать бинарный поиск.

Альтернативная интерпретация:

* «убиваем» все элементы,
* «оживляем» их в порядке возрастания,
* используем структуру данных:

  * дерево отрезков или Фенвика,
  * операции:

    * максимум на префиксе,
    * обновление в точке.

---

## 8. Задача О Рюкзаке (0/1 Knapsack)

Дано:

* `N` предметов,
* у каждого есть вес `w[i]` и стоимость `c[i]`,
* рюкзак вместимости `W`.

Нужно:

* выбрать подмножество предметов,
* суммарный вес ≤ `W`,
* суммарная стоимость — максимальна.

---

### Динамика

**Состояние**:

```
DP[i][w] — максимальная стоимость,
используя первые i предметов и суммарный вес w
```

**База**:

```
DP[0][0] = 0
остальные = −∞
```

**Переход**:

* не берём предмет `i`:

```
DP[i][w] = DP[i-1][w]
```

* берём предмет `i`:

```
DP[i][w] = DP[i-1][w - weight[i]] + cost[i]
```

**Ответ**:

```
max DP[N][w], где w ≤ W
```

Сложность: `O(N · W)`.

---

## 9. Общие Выводы Лекции

* Главное в ДП — **правильно выбрать состояние**.
* Хранить нужно **минимально достаточную информацию**.
* Почти всегда:

  * есть база,
  * есть переход,
  * корректность доказывается индукцией.
* Одну и ту же задачу можно решать:

  * «назад»,
  * «вперёд»,
  * разными динамиками с разной асимптотикой.

---

