# Лекция 2: Динамика (продолжение) И Матрицы

## 1. Задача О Рюкзаке (Дополнения)

В прошлой лекции обсуждалась классическая задача о рюкзаке: есть $N$ предметов с весом $w_i$ и стоимостью $c_i$. Ограничение по весу $W$.

### Оптимизация По Памяти
Классическая динамика $dp[i][w]$ (максимальная стоимость для первых $i$ предметов с суммарным весом $w$) требует $O(N \cdot W)$ памяти.
*   **Наблюдение:** Для вычисления $i$-го слоя нужны значения только $i-1$-го слоя.
*   **Решение:** Можно хранить только два слоя (текущий и предыдущий) и менять их местами циклически.
*   **Результат:** Память сокращается до линейной $O(W)$.

### Обратная Динамика (Оптимизация При Больших весах)
Если веса $W$ очень большие (например, $10^9$), но стоимости предметов $c_i$ маленькие.
*   **Новое состояние:** $dp[i][v]$ — *минимальный* вес предметов, необходимый для получения суммарной стоимости ровно $v$, используя первые $i$ предметов.
*   **Переход:** $dp[i][v] = \min(dp[i-1][v], dp[i-1][v - c_i] + w_i)$.
*   **Сложность:** $O(N \cdot \sum c_i)$. Это выгодно, если сумма стоимостей меньше $W$.

---

## 2. ДП С Помощью Матриц (Числа Фибоначчи)

Задача: Найти $N$-е число Фибоначчи по модулю $M$ для очень большого $N$.
Обычная формула $F_n = F_{n-1} + F_{n-2}$ дает решение за $O(N)$. Нужно быстрее.

### Матричный Подход
Запишем переход от пары $(F_n, F_{n-1})$ к $(F_{n+1}, F_n)$ в виде умножения на матрицу перехода:
$$
\begin{pmatrix} F_{n+1} \\ F_n \end{pmatrix} = \begin{pmatrix} 1 & 1 \\ 1 & 0 \end{pmatrix} \times \begin{pmatrix} F_n \\ F_{n-1} \end{pmatrix}
$$
Чтобы получить $N$-е число, нужно возвести матрицу в степень $N-1$:
$$
\begin{pmatrix} F_{N} \\ F_{N-1} \end{pmatrix} = \begin{pmatrix} 1 & 1 \\ 1 & 0 \end{pmatrix}^{N-1} \times \begin{pmatrix} F_1 \\ F_0 \end{pmatrix}
$$

### Бинарное Возведение В Степень (Binary Exponentiation)
Позволяет возвести число (или матрицу) $A$ в степень $N$ за $O(\log N)$.
*   Если $N$ четное: $A^N = (A^{N/2})^2$.
*   Если $N$ нечетное: $A^N = A \cdot A^{N-1}$.

**Сложность для матриц:**
Умножение двух матриц размера $K \times K$ занимает $O(K^3)$.
Итоговая сложность поиска $N$-го числа Фибоначчи: **$O(K^3 \log N)$**, где $K=2$.

### Сложные Рекурренты
Если дана рекуррента вида $a_n = \lambda a_{n-1} + \mu a_{n-2} + 1$ (с константой).
Чтобы учесть «+1», вектор состояния расширяют, добавляя фиктивную единицу:
$$
\begin{pmatrix} a_n \\ a_{n-1} \\ 1 \end{pmatrix} = \begin{pmatrix} \lambda & \mu & 1 \\ 1 & 0 & 0 \\ 0 & 0 & 1 \end{pmatrix} \times \begin{pmatrix} a_{n-1} \\ a_{n-2} \\ 1 \end{pmatrix}
$$

---

## 3. Графы И Пути Фиксированной Длины

Дан граф (ориентированный или нет) с $N$ вершинами. Матрица смежности $M$, где $M_{uv} = 1$, если есть ребро $u \to v$, иначе 0.

### Теорема
Значение в ячейке $(u, v)$ матрицы $M^K$ (матрица смежности, возведенная в степень $K$) равно **количеству путей длины ровно $K$** из вершины $u$ в вершину $v$.

*   **Доказательство:** Основано на определении умножения матриц. $(M^2)_{uv} = \sum_k M_{uk} \cdot M_{kv}$. Это сумма путей через все промежуточные вершины $k$.
*   **Сложность:** $O(N^3 \log K)$.

---

## 4. Количество Путей Длины Не Более K

Задача: Найти количество путей из $X$ в $Y$ длины $\le K$.
Это эквивалентно вычислению суммы степеней матрицы: $S = M^0 + M^1 + M^2 + \dots + M^K$.

Прямая формула суммы геометрической прогрессии не работает из-за сложности с делением (обратной матрицей) по модулю.

**Рекурсивный подход (Divide & Conquer):**
Вводим функцию, которая возвращает пару значений: $\{M^K, \text{Sum}(K)\}$.
*   Для четного $K$: сводим к задаче для $K/2$, комбинируем результаты.
*   Пример логики: $M^0 + \dots + M^{2k+1} = (M^0 + \dots + M^k) \cdot (E + M^{k+1})$.
*   Сложность остается $O(N^3 \log K)$.

---

## 5. Проверка Существования Пути (Boolean Matrix Multiplication)

Задача: Узнать, существует ли путь длины $K$ из $X$ в $Y$ (Да/Нет).

### Булево Умножение
Модифицируем правила умножения матриц:
*   Вместо суммы ($+$) $\to$ Дизъюнкция (OR, $\lor$).
*   Вместо произведения ($\times$) $\to$ Конъюнкция (AND, $\land$).

Тогда $(M^K)_{uv} = 1$, если путь существует.

### Оптимизация (Bitset / Machine word)
Так как матрица состоит из 0 и 1, можно сжать строку матрицы в битовую маску (используя `int` или `long long` как контейнер для $W$ бит, где $W=32$ или $64$).
*   Операции OR и AND над машинными словами выполняются за $O(1)$.
*   Внутренний цикл умножения сокращается в $W$ раз.
*   **Итоговая сложность:** $O(\frac{N^3}{W} \log K)$. На практике это дает существенное ускорение (в 32-64 раза).

---

## 6. Кратчайшие Пути Фиксированной Длины (Min-Plus Multiplication)

Задача: Взвешенный граф. Найти путь из $X$ в $Y$ длины ровно $K$ с **минимальным** суммарным весом ребер.

### Min-Plus Произведение
Определим новую операцию умножения матриц $\otimes$:
$$
(A \otimes B)_{ij} = \min_{k} (A_{ik} + B_{kj})
$$
*   Вместо суммы $\to$ Минимум ($\min$).
*   Вместо умножения $\to$ Сложение ($+$).
*   Вместо 0 (отсутствие ребра) используем $+\infty$.

Эта операция ассоциативна, поэтому применимо **бинарное возведение в степень**.
Если возвести матрицу весов $M$ в степень $K$ с помощью Min-Plus умножения, то $(M^K)_{uv}$ будет равно минимальному весу пути длины $K$.

**Сложность:** $O(N^3 \log K)$.