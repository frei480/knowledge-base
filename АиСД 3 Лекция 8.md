# Лекция 8: Приложения FFT: Операции Над Многочленами

**Введение и контекст**
*   Для удобства предполагается, что вычисления происходят в поле $\mathbb{Z}_p$ (где $p$ подобрано специальным образом для FFT, $p = c \cdot 2^k + 1$), чтобы избежать ошибок округления.
*   Если нужны вещественные числа или большие коэффициенты, можно использовать несколько разных модулей и Китайскую теорему об остатках (CRT).
*   Везде далее $N$ — степень многочлена, а сложность базового умножения многочленов — $O(N \log N)$.

---

## 1. Обратный Многочлен По Модулю $x^m$

**Задача:**
Дан многочлен $P(x)$. Найти многочлен $Q(x)$ такой, что $P(x) \cdot Q(x) \equiv 1 \pmod{x^m}$.
Это означает, что произведение равно $1 + x^m \cdot (\dots)$.

**Условие существования:**
Обратный элемент существует тогда и только тогда, когда свободный член $P(0) \neq 0$. В поле $\mathbb{Z}_p$ это значит $a_0 \neq 0$.

**Алгоритм (Итеративное удвоение точности):**
Используется идея, аналогичная методу Ньютона.
1.  **База ($m=1$):** Нужно найти обратное к свободному члену число в $\mathbb{Z}_p$. $Q_0 = (a_0)^{-1} \pmod p$.
2.  **Шаг перехода ($m \to 2m$):**
    *   Пусть мы знаем $Q_0$ такое, что $P Q_0 \equiv 1 \pmod{x^m}$.
    *   Ищем $Q$ для модуля $x^{2m}$.
    *   Формула перехода:
        $$Q = Q_0(2 - P Q_0) \pmod{x^{2m}}$$
    *   Для вычисления нужно перемножить многочлены и вычесть их. Все операции проводятся по модулю $x^{2m}$ (старшие степени отбрасываются).

**Сложность:**
На каждом шаге происходит умножение многочленов. Размер удваивается ($1, 2, 4, \dots, m$). Сумма ряда $N \log N + \frac{N}{2} \log \frac{N}{2} + \dots$ сходится к **$O(N \log N)$**.

---

## 2. Деление Многочленов С Остатком

**Задача:**
Даны $F$ и $G$. Найти частное $Q$ и остаток $R$ такие, что $F = Q \cdot G + R$, где $\deg R < \deg G$.

**Идея (Разворот коэффициентов):**
Вводится операция $rev(P)$ — запись коэффициентов многочлена в обратном порядке (эквивалентно $x^{\deg P} P(1/x)$).
Равенство деления можно переписать для развернутых многочленов:
$$rev(F) \equiv rev(Q) \cdot rev(G) \pmod{x^{\deg F - \deg G + 1}}$$

**Алгоритм:**
1.  Вычисляем $rev(F)$ и $rev(G)$.
2.  Находим обратный многочлен к $rev(G)$ по модулю $x^{\deg F - \deg G + 1}$ (используя алгоритм из п. 1). Примечание: у $rev(G)$ свободный член не равен 0, так как это старший член исходного $G$, поэтому обратный существует.
3.  Находим $rev(Q) = rev(F) \cdot (rev(G))^{-1}$.
4.  Разворачиваем $rev(Q)$ обратно, получаем $Q$.
5.  Находим остаток: $R = F - Q \cdot G$.

**Сложность:** **$O(N \log N)$**.

---

## 3. Multipoint Evaluation (Вычисление Значений В точках)

**Задача:**
Дан многочлен $P(x)$ степени $N-1$ и набор точек $x_1, \dots, x_N$. Найти значения $P(x_i)$ для всех $i$.
Наивное решение — $O(N^2)$. Нужно быстрее.

**Идея (Дерево отрезков на многочленах):**
Используется свойство остатка от деления: $P(a) = P(x) \pmod{x-a}$.
1.  Строим "дерево":
    *   В листьях лежат многочлены $(x - x_i)$.
    *   В каждой внутренней вершине лежит произведение многочленов её детей.
    *   В корне — произведение $(x-x_i)$ по всем $i$.
    *   Построение идет снизу вверх, перемножением.
2.  Спускаемся сверху вниз:
    *   В корне берем $P \pmod{\text{многочлен корня}}$ (если степень $P$ велика).
    *   Передаем результат детям, беря его по модулю многочлена, хранящегося в ребенке.
    *   В листьях получаем многочлены степени 0 (константы) — это и есть значения $P(x_i)$.

**Сложность:**
*   Построение дерева: $O(N \log^2 N)$ (на каждом уровне суммарная длина $N$, уровней $\log N$, умножение стоит логарифм).
*   Спуск (взятие остатков): тоже **$O(N \log^2 N)$**.

---

## 4. Интерполяция

**Задача:**
Даны $N$ пар $(x_i, y_i)$. Восстановить многочлен $P(x)$ степени $N-1$, проходящий через эти точки.

**Алгоритм (Разделяй и властвуй):**
1.  Делим точки на две половины: $L$ (левая) и $R$ (правая).
2.  Рекурсивно строим многочлен $Q_1(x)$, интерполирующий точки из $L$.
3.  Ищем $P(x)$ в виде:
    $$P(x) = Q_1(x) + M_L(x) \cdot Q_2(x)$$
    где $M_L(x) = \prod_{x_j \in L} (x - x_j)$ (это многочлен из дерева Multipoint evaluation).
4.  Для точек из $R$ должно выполняться:
    $$y_k = Q_1(x_k) + M_L(x_k) \cdot Q_2(x_k)$$
    Отсюда находим требуемые значения для $Q_2$ в точках правой половины:
    $$Q_2(x_k) = \frac{y_k - Q_1(x_k)}{M_L(x_k)}$$
5.  Вычисляем значения $Q_1(x)$ и $M_L(x)$ во всех точках $R$ с помощью *Multipoint Evaluation*.
6.  Рекурсивно решаем задачу интерполяции для $Q_2$ на наборе точек $R$ с новыми значениями.

**Сложность:** **$O(N \log^2 N)$**.

---

## 5. Линейные Рекурренты (поиск $N$-го члена)

**Задача:**
Дана последовательность $A_n = \sum_{i=1}^{S} c_{i-1} A_{n-i}$ (линейная рекуррента порядка $S$). Найти $A_N$ для очень большого $N$.

**Сравнение подходов:**
*   Матричное умножение: $O(S^3 \log N)$.
*   Наше решение с полиномами: $O(S \log S \log N)$.

**Алгоритм:**
1.  Записываем характеристический многочлен рекурренты:
    $$P(x) = x^S - \sum_{i=0}^{S-1} c_i x^i$$
    *(Примечание: в лекции коэффициенты и степени записаны в общем виде, суть в том, что $x^S$ выражается через младшие степени).*
2.  **Ключевое наблюдение:**
    Если $x^N \equiv \sum_{i=0}^{S-1} b_i x^i \pmod{P(x)}$, то $N$-й член рекурренты выражается через первые $S$ членов с теми же коэффициентами $b_i$:
    $$A_N = \sum_{i=0}^{S-1} b_i A_i$$
3.  Вычисляем $x^N \pmod{P(x)}$ с помощью бинарного возведения в степень.
    *   Операция: умножение многочленов и взятие остатка от деления на $P(x)$.
    *   Размер многочленов всегда $\approx S$.
    *   Стоимость одной операции: $O(S \log S)$.
    *   Количество шагов: $\log N$.

**Итоговая сложность:** **$O(S \log S \log N)$**.