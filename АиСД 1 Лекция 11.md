Вот подробный конспект-пересказ 11-й лекции по курсу «Алгоритмы и структуры данных».

---

# Лекция 11: Асимптотика Splay-деревьев (продолжение) И Неявный Ключ

## Часть 1. Завершение Анализа Асимптотики Splay-дерева

Лекция начинается с продолжения доказательства того, что учётная (амортизированная) стоимость операций в Splay-дереве составляет $O(\log N)$.

### Метод Потенциалов (напоминание)
*   **Ранг вершины** $R(x) = \log_2(\text{Size}(subtree(x)))$.
*   **Потенциал** $\Phi = \sum R(x)$ (сумма рангов всех вершин).
*   **Учётная стоимость** = Реальная стоимость + $\Delta \Phi$.
*   На прошлой лекции доказали для операции **Zig** (один поворот у корня):
    $$Cost_{zig} \le 1 + 3(R'(x) - R(x))$$

### Анализ Операций Zig-Zig И Zig-Zag
Требуется доказать аналогичные оценки для более сложных вращений, чтобы избавиться от константы «1» в формуле (кроме последнего шага), что позволит свернуть сумму в телескопическую.

#### 1. Операция Zig-Zig
*   **Ситуация:** $x$ — не корень, его родитель $P$ и дедушка $G$ находятся по одну сторону (оба левые сыновья или оба правые).
*   **Действие:** Два однотипных поворота. Сначала вращаем $P-G$, потом $x-P$.
*   **Реальная стоимость:** 2 (два поворота).
*   **Изменение потенциала:** Меняются ранги только у $x, P, G$. При этом $R'(x) = R(G)$ (так как $x$ занимает место дедушки и забирает всё поддерево).
*   **Доказательство:**
    Используется неравенство логарифмов: если $a+b \le 1$, то $\log a + \log b \le -2$.
    В ходе преобразований лектор показывает, что слагаемые сокращаются так, что «съедают» двойку из реальной стоимости.
*   **Итог для Zig-Zig:**
    $$Cost_{zig-zig} \le 3(R'(x) - R(x))$$
    *Важно:* здесь нет «+1», в отличие от Zig.

#### 2. Операция Zig-Zag
*   **Ситуация:** $x$ и $P$ — разносторонние сыновья (например, $P$ — левый сын, $x$ — правый).
*   **Действие:** Двойной поворот (сначала $x$ меняется с $P$, потом $x$ с $G$). $x$ всплывает вверх.
*   **Анализ:** Аналогичен Zig-Zig. Используется то же неравенство для выпуклых функций.
*   **Итог для Zig-Zag:**
    $$Cost_{zig-zag} \le 2(R'(x) - R(x)) \le 3(R'(x) - R(x))$$
    Оценку загрубляют до 3, чтобы формулы были единообразными.

### Итоговая Оценка Операции Splay(x)
Операция `Splay(x)` состоит из последовательности шагов Zig-Zig/Zig-Zag и, возможно, одного шага Zig в самом конце (если расстояние до корня нечетное).

Суммируем учётные стоимости всех шагов:
*   Это **телескопическая сумма**: $3(R_1 - R_0) + 3(R_2 - R_1) + \dots$
*   Промежуточные ранги сокращаются.
*   Остается:
    $$Cost_{Splay} \le 1 + 3(R_{final}(x) - R_{initial}(x))$$
*   Так как ранг — это логарифм размера поддерева, он не превышает $\log N$.
*   **Вывод:** Амортизированная стоимость `Splay` равна **$O(\log N)$**.

---

## Часть 2. Асимптотика Стандартных Операций (Find, Insert, Erase)

Лектор доказывает, что остальные операции тоже работают за амортизированный логарифм, так как они сводятся к Splay и небольшому изменению потенциала.

1.  **Find(x):**
    *   Спускаемся до вершины (реальное время пропорционально глубине).
    *   Вызываем `Splay` от найденной (или последней посещенной) вершины.
    *   В анализе возникает нюанс: реальное время спуска нужно компенсировать. Для этого потенциал дерева можно «удвоить» ($\Phi_{new} = 2\Phi$), тогда математика сходится.
    *   Итог: $O(\log N)$.

2.  **Insert(x):**
    *   Спускаемся, подвешиваем лист.
    *   Потенциал увеличивается не сильно (добавляется слагаемое $\log(\text{size})$ для вершин на пути вставки). Сумма изменений $\approx O(\log N)$.
    *   Затем вызывается `Splay`, который тоже $O(\log N)$.
    *   Итог: $O(\log N)$.

3.  **Erase(x):**
    *   Находим, удаляем (через Splay/Merge), потенциал меняется незначительно.
    *   Итог: $O(\log N)$.

---

## Часть 3. Операции Merge И Split В Splay-дереве

Splay-деревья (как и Декартовы деревья) отлично поддерживают разрезание и слияние.

### Merge (Слияние)
*   **Условие:** Все ключи в дереве $A$ меньше всех ключей в дереве $B$.
*   **Алгоритм:**
    1.  Найти максимум в дереве $A$ (идти вправо до упора).
    2.  Сделать `Splay` от этого максимума. Теперь он в корне $A$, и у него **нет правого сына**.
    3.  Подвесить дерево $B$ как правого сына к корню $A$.
*   **Сложность:** $O(\log N)$ (определяется стоимостью Splay).

### Split (Разрезание)
*   **Задача:** Разбить дерево по ключу $K$ на два: $T_1$ (ключи $\le K$) и $T_2$ (ключи $> K$).
*   **Алгоритм:**
    1.  Найти в дереве максимальный элемент $x$, который $\le K$ (обычный спуск BST).
    2.  Сделать `Splay(x)`. Теперь $x$ в корне.
    3.  В правом поддереве корня $x$ гарантированно все элементы $> x$, а так как $x$ был максимальным из $\le K$, то справа все $> K$.
    4.  Отцепляем правого сына $x$. Дерево распалось на два.
*   **Сложность:** $O(\log N)$.

---

## Часть 4. Неявный Ключ (Implicit Splay Tree)

Лектор переходит к задаче поддержки массива с возможностью вставки/удаления в произвольную позицию и вычисления функций на отрезках.

### Проблема Обычных Массивов И BST
*   В массиве вставка в середину требует сдвига всех элементов ($O(N)$).
*   В обычном BST ключи фиксированы. Если ключом сделать индекс, то при вставке в начало у всех элементов индексы должны сдвинуться ($+1$). Обновлять ключи во всем дереве долго.

### Идея Неявного Ключа
Используем Splay (или Treap), но **не храним ключи (индексы) явно**.
*   Порядок элементов в массиве определяется **in-order обходом** дерева (слева-направо).
*   В каждой вершине храним дополнительное поле: **`Size`** (размер поддерева).
*   **Индекс элемента** определяется «на лету»:
    *   Индекс корня поддерева = `Size(LeftChild)`.
    *   Если идем в правое поддерево, к локальному индексу прибавляем (`Size(LeftChild)` + 1).

### Операции С Неявным Ключом

1.  **Insert(pos, val):**
    *   Используем `Split` по «неявному индексу».
    *   Разрезаем дерево по позиции `pos` на $L$ и $R$.
    *   Создаем новую вершину с `val`.
    *   Делаем `Merge(L, NewNode)`, затем `Merge(Result, R)`.
    *   *Важно:* При вращениях в Splay нужно пересчитывать поле `Size`.

2.  **Erase(pos):**
    *   `Split` по `pos` $\to$ выделяем элемент.
    *   `Split` по `pos+1` $\to$ отделяем его от правой части.
    *   Удаляем вершину, делаем `Merge` оставшихся частей.

3.  **GetSum(Left, Right) / RMQ и другие функции на отрезке:**
    *   В каждой вершине храним, например, `Sum` (сумма значений в поддереве).
    *   При вращениях `Sum` легко пересчитывается через сыновей:
        `Node.Sum = Node.Value + Left.Sum + Right.Sum`.
    *   Чтобы узнать сумму на отрезке $[L, R]$:
        *   **Способ 1 (через Split):** Вырезать кусок $[L, R]$ сплитами, посмотреть `Sum` в корне этого куска, потом склеить обратно.
        *   **Способ 2 (через Splay — более изящный):**
            1.  `Splay` элемента с индексом $L-1$. Теперь все элементы $< L$ в левом поддереве корня.
            2.  `Splay` элемента с индексом $R+1$ **внутри правого поддерева** корня.
            3.  В итоге нужный отрезок $[L, R]$ окажется **левым сыном** правого сына корня. Берем сумму из этой вершины.

4.  **Update(pos, val):**
    *   Найти элемент по неявному индексу (`Find`), изменить значение.
    *   Поднять его `Splay` до корня, попутно обновляя суммы (или просто обновить значения вверх по пути).

### Резюме По Неявному Ключу
Позволяет делать операции массива (вставка, удаление, сумма на отрезке, разворот отрезка и т.д.) за **$O(\log N)$**. Splay-дерево удобно тем, что нужный элемент или отрезок всегда поднимается наверх, и инварианты легко поддерживать.