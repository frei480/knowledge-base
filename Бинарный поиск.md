<u>Задача:</u> Есть массив a неубывающих чисел, нужно узнать есть ли x в этом массиве.
```python
left=0
right=len(a) - 1
while (right - left > 1):
	mid = (right - left)//2+left
	if (a[mid] > x):
		right = mid
	elif (a[mid] < x):
		left = mid	
if (a[left] == x): # нашли
else # не нашли
```
Асимпотитка: $O(log n)$ на запрос.